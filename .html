<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Global SSC — Build Your Dreams Into a Website</title>
<meta name="description" content="Global SSC designs and builds websites that turn your ideas into reality. Chat with us on WhatsApp to get started.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root{
    --space:#08051C;
    --space-2:#0D0830;
    --violet:#8965FF;
    --violet-light:#A994FF;
    --cyan:#65FFCF;
    --magenta:#FF4FCB;
    --cream:#F1EEFF;
    --wa-green:#25D366;
    --line: rgba(169,148,255,0.18);
  }
  *{box-sizing:border-box; margin:0; padding:0;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--space);
    color:var(--cream);
    font-family:'Inter', sans-serif;
    line-height:1.6;
    overflow-x:hidden;
  }
  h1,h2,h3{ font-family:'Space Grotesk', sans-serif; font-weight:700; letter-spacing:-0.02em;}
  .mono{ font-family:'IBM Plex Mono', monospace; letter-spacing:0.08em; }
  a{ color:inherit; text-decoration:none; }

  header{
    position:fixed; top:22px; left:50%; transform:translateX(-50%);
    z-index:100; width:min(920px, 90vw);
    display:flex; align-items:center; justify-content:space-between;
    padding:14px 24px;
    background:rgba(13,8,48,0.55);
    border:1px solid var(--line);
    border-radius:999px;
    backdrop-filter: blur(16px);
  }
  .logo{ display:flex; align-items:center; gap:10px; font-size:1rem; font-weight:700; letter-spacing:-0.01em;}
  .logo-mark{
    width:30px; height:30px; border-radius:9px;
    background:linear-gradient(135deg, var(--violet), var(--magenta));
    display:flex; align-items:center; justify-content:center;
    font-family:'IBM Plex Mono', monospace; font-size:0.6rem; color:var(--space);
    font-weight:700;
    box-shadow:0 0 18px rgba(137,101,255,0.55);
  }
  nav{ display:flex; gap:30px; font-size:0.86rem; }
  nav a{ opacity:0.78; transition:opacity .2s, color .2s; }
  nav a:hover{ opacity:1; color:var(--cyan); }
  .nav-cta{
    display:flex; align-items:center; gap:8px;
    background:var(--wa-green); color:#08051C; font-weight:600;
    padding:9px 18px; border-radius:100px; font-size:0.82rem;
    box-shadow:0 0 18px rgba(37,211,102,0.35);
  }
  .nav-links{ display:flex; align-items:center; gap:26px; }
  @media (max-width:820px){ nav{display:none;} header{ width:92vw; padding:12px 18px;} }

  .hero{
    position:relative; min-height:100svh;
    display:block; text-align:center;
    padding: 130px 6vw 70px;
    overflow:hidden;
    background:
      radial-gradient(circle at 50% 40%, rgba(137,101,255,0.35), transparent 32%),
      radial-gradient(circle at 78% 62%, rgba(255,79,203,0.28), transparent 34%),
      radial-gradient(circle at 20% 70%, rgba(101,255,207,0.14), transparent 30%),
      var(--space);
  }
  #starCanvas{ position:absolute; inset:0; width:100%; height:100%; display:block; z-index:0; }

  .hero-skyline{
    position:absolute; left:0; right:0; bottom:0; height:130px; z-index:1;
    display:flex; align-items:flex-end; justify-content:space-between; padding:0 4vw;
    opacity:0.55; pointer-events:none;
  }
  .hero-skyline span{
    flex:1; margin:0 3px;
    background:linear-gradient(180deg, rgba(137,101,255,0.04), rgba(255,79,203,0.28));
    box-shadow:0 0 14px rgba(255,79,203,0.22);
  }
  .hero-skyline span:nth-child(odd){ height:55%; }
  .hero-skyline span:nth-child(3n){ height:82%; }
  .hero-skyline span:nth-child(4n){ height:38%; }
  .hero-skyline span:nth-child(5n){ height:100%; }

  .floating-panel{
    position:absolute; z-index:1; display:flex; flex-direction:column; gap:4px; text-align:left;
    padding:12px 16px; border:1px solid rgba(151,125,255,0.28); border-radius:12px;
    background:rgba(10,5,35,0.45); backdrop-filter:blur(10px);
    animation:floatPanel 6s ease-in-out infinite;
  }
  .panel-label{ font-size:0.6rem; color:rgba(199,192,255,0.7); letter-spacing:0.1em; }
  .panel-value{ font-family:'Space Grotesk', sans-serif; font-weight:700; color:var(--cyan); font-size:1.1rem; }
  .panel-a{ top:22%; left:5vw; animation-delay:0s; }
  .panel-b{ top:34%; right:5vw; animation-delay:1.5s; }
  .panel-c{ bottom:14%; left:7vw; animation-delay:3s; }
  @keyframes floatPanel{ 0%,100%{ transform:translateY(0); } 50%{ transform:translateY(-10px); } }
  @media (max-width:900px){ .floating-panel{ display:none; } }
  @media (prefers-reduced-motion: reduce){ .floating-panel{ animation:none; } }

  .hero-label{
    position:relative; z-index:2;
    display:inline-flex; align-items:center; gap:9px;
    color:#c7c0ff; font-family:'IBM Plex Mono', monospace; font-size:0.68rem;
    letter-spacing:0.12em; padding:8px 16px; margin-bottom:34px;
    border:1px solid rgba(151,125,255,0.3); border-radius:999px;
    background:rgba(10,5,35,0.4); backdrop-filter:blur(12px);
  }
  .live-dot{
    width:7px; height:7px; border-radius:50%; background:var(--cyan);
    box-shadow:0 0 8px var(--cyan), 0 0 18px var(--cyan);
    animation:liveBlink 1.6s infinite;
  }
  @keyframes liveBlink{ 50%{ opacity:0.3; } }

  .hero-title{
    position:relative; z-index:2; text-align:center;
    font-size:clamp(2.6rem, 8vw, 6rem); line-height:0.94; letter-spacing:-0.03em;
    text-shadow:0 0 40px rgba(111,78,255,0.25);
    max-width:16ch;
  }
  .hero-title .accent{
    color:var(--violet-light);
    text-shadow:0 0 22px rgba(137,101,255,0.5), 0 0 70px rgba(76,48,255,0.25);
  }

  .cube-stage{
    position:relative; z-index:2;
    width:220px; height:220px; margin:52px auto 20px;
    perspective:1000px;
    contain:layout paint;
    isolation:isolate;
  }
  .cube{
    width:100%; height:100%; position:relative;
    transform-style:preserve-3d;
    animation: spin 14s linear infinite;
    will-change:transform;
  }
  @keyframes spin{
    from{ transform:rotateX(-18deg) rotateY(0deg); }
    to{ transform:rotateX(-18deg) rotateY(360deg); }
  }
  .face{
    position:absolute; width:220px; height:220px;
    border:1px solid rgba(200,190,255,0.45);
    display:flex; align-items:center; justify-content:center;
    overflow:hidden;
    font-family:'IBM Plex Mono', monospace; font-size:0.7rem; color:rgba(230,225,255,0.9);
  }
  .face-front{ transform:translateZ(110px); background:radial-gradient(circle at 50% 40%, rgba(101,255,207,0.2), rgba(8,5,28,0.85)); }
  .face-back{ transform:rotateY(180deg) translateZ(110px); background:radial-gradient(circle at 50% 85%, rgba(255,79,203,0.2), rgba(8,5,28,0.85)); }
  .face-right{ transform:rotateY(90deg) translateZ(110px); background:radial-gradient(circle at 30% 30%, rgba(137,101,255,0.25), rgba(8,5,28,0.82)); }
  .face-left{ transform:rotateY(-90deg) translateZ(110px); background:radial-gradient(circle at 70% 70%, rgba(101,255,207,0.16), rgba(8,5,28,0.85)); }
  .face-top{ transform:rotateX(90deg) translateZ(110px); background:radial-gradient(circle at 50% 50%, rgba(230,225,255,0.15), rgba(8,5,28,0.85)); }
  .face-bottom{ transform:rotateX(-90deg) translateZ(110px); background:radial-gradient(circle at 50% 50%, rgba(255,79,203,0.18), rgba(8,5,28,0.86)); }

  .face-tag{
    position:absolute; bottom:10px; left:50%; transform:translateX(-50%);
    font-size:0.58rem; letter-spacing:0.12em; color:rgba(230,225,255,0.85);
    background:rgba(8,5,28,0.6); padding:3px 9px; border-radius:100px;
    border:1px solid rgba(200,190,255,0.28); white-space:nowrap;
  }

  /* Face 1: holographic Earth */
  .face-earth{ position:relative; width:118px; height:118px; }
  .earth-globe{
    width:100%; height:100%; border-radius:50%;
    background:
      repeating-linear-gradient(0deg, rgba(101,255,207,0.35) 0 1px, transparent 1px 13px),
      radial-gradient(circle at 35% 30%, rgba(101,255,207,0.55), rgba(20,45,60,0.9) 60%, rgba(8,5,28,1) 100%);
    box-shadow:0 0 30px rgba(101,255,207,0.4), inset -14px -14px 30px rgba(0,0,0,0.6);
    animation: earthSpin 6s linear infinite;
  }
  @keyframes earthSpin{ from{ background-position:0 0, 0 0; } to{ background-position:118px 0, 0 0; } }
  .earth-ring{
    position:absolute; inset:-14px; border:1px solid rgba(101,255,207,0.3); border-radius:50%;
    animation:ringPulse 3s ease-in-out infinite;
  }
  @keyframes ringPulse{ 0%,100%{ transform:scale(1); opacity:0.6; } 50%{ transform:scale(1.08); opacity:0.15; } }

  /* Face 2: neon city skyline */
  .face-city{ display:flex; align-items:flex-end; gap:6px; height:104px; }
  .city-bar{
    width:13px; height:var(--h);
    background:linear-gradient(180deg, rgba(255,79,203,0.15), rgba(255,79,203,0.6));
    box-shadow:0 0 10px rgba(255,79,203,0.5);
    animation:cityFlicker 2.4s ease-in-out infinite; animation-delay:var(--d);
    position:relative;
  }
  .city-bar::before{
    content:''; position:absolute; inset:2px;
    background-image:repeating-linear-gradient(0deg, rgba(255,255,255,0.55) 0 2px, transparent 2px 9px);
    opacity:0.5;
  }
  @keyframes cityFlicker{ 0%,100%{ opacity:1; } 50%{ opacity:0.55; } }

  /* Face 3: floating UI panel */
  .face-ui{ width:148px; display:flex; flex-direction:column; gap:12px; }
  .ui-bar{
    height:6px; width:var(--w); border-radius:4px;
    background:linear-gradient(90deg, var(--violet-light), var(--cyan));
    box-shadow:0 0 8px rgba(137,101,255,0.5);
    animation:uiPulse 2.6s ease-in-out infinite;
  }
  .ui-dot{
    width:32px; height:32px; border-radius:50%; align-self:flex-end;
    border:2px solid var(--cyan); position:relative;
  }
  .ui-dot::after{
    content:''; position:absolute; inset:8px; border-radius:50%;
    background:var(--cyan); box-shadow:0 0 12px var(--cyan);
    animation:liveBlink 1.6s infinite;
  }
  @keyframes uiPulse{ 0%,100%{ opacity:0.5; } 50%{ opacity:1; } }

  /* Face 4: data grid / network */
  .face-grid-net{ display:grid; grid-template-columns:repeat(3,1fr); gap:22px; }
  .face-grid-net span{
    width:8px; height:8px; border-radius:50%; background:var(--violet-light);
    box-shadow:0 0 10px var(--violet-light);
    animation:nodePulse 1.8s ease-in-out infinite;
  }
  .face-grid-net span:nth-child(2n){ animation-delay:.3s; background:var(--cyan); box-shadow:0 0 10px var(--cyan); }
  .face-grid-net span:nth-child(3n){ animation-delay:.6s; }
  @keyframes nodePulse{ 0%,100%{ transform:scale(1); opacity:0.5; } 50%{ transform:scale(1.6); opacity:1; } }

  /* Face 5: radar scan */
  .face-radar{ position:relative; width:128px; height:128px; }
  .radar-ring{ position:absolute; inset:0; border:1px solid rgba(101,255,207,0.35); border-radius:50%; }
  .radar-ring.r2{ inset:22px; }
  .radar-sweep{
    position:absolute; inset:0; border-radius:50%;
    background:conic-gradient(from 0deg, rgba(101,255,207,0.55), transparent 30%);
    animation:radarSpin 3s linear infinite;
  }
  @keyframes radarSpin{ to{ transform:rotate(360deg); } }

  /* Face 6: brand mark */
  .face-brand{
    font-family:'Space Grotesk', sans-serif; font-weight:700; font-size:1.1rem;
    text-align:center; color:var(--cream); text-shadow:0 0 18px rgba(137,101,255,0.6);
  }

  .cube-shadow{
    width:180px; height:26px; margin:0 auto; border-radius:50%;
    background:radial-gradient(ellipse, rgba(137,101,255,0.35), transparent 70%);
    filter:blur(4px);
  }

  .hero-sub{
    position:relative; z-index:2; text-align:center; max-width:520px; margin:24px auto 0;
    color:rgba(241,238,255,0.75); font-size:1.02rem;
  }
  .hero-ctas{ position:relative; z-index:2; display:flex; gap:16px; margin-top:36px; flex-wrap:wrap; justify-content:center; }
  .btn-primary{
    background:var(--wa-green); color:#08051C; font-weight:600;
    padding:16px 28px; border-radius:100px; display:inline-flex; align-items:center; gap:10px;
    font-size:0.96rem; transition:transform .2s ease, box-shadow .2s ease;
    box-shadow:0 0 24px rgba(37,211,102,0.35);
  }
  .btn-primary:hover{ transform:translateY(-2px); box-shadow:0 10px 30px rgba(37,211,102,0.4);}
  .btn-secondary{
    border:1px solid var(--line); color:var(--cream);
    padding:16px 28px; border-radius:100px; font-size:0.96rem;
    background:rgba(255,255,255,0.02);
    transition:border-color .2s ease, color .2s ease;
  }
  .btn-secondary:hover{ border-color:var(--violet-light); color:var(--violet-light); }

  .hero-coords{
    position:absolute; z-index:2; right:5vw; bottom:6vh;
    color:rgba(190,183,255,0.5); font-size:0.62rem; line-height:1.9; text-align:right;
  }
  @media (max-width:800px){
    .hero-coords{ display:none; }
    .cube-stage{ width:160px; height:160px; } .face{ width:160px; height:160px; }
    .face-front{ transform:translateZ(80px); } .face-back{ transform:rotateY(180deg) translateZ(80px);}
    .face-right{ transform:rotateY(90deg) translateZ(80px);} .face-left{ transform:rotateY(-90deg) translateZ(80px);}
    .face-top{ transform:rotateX(90deg) translateZ(80px);} .face-bottom{ transform:rotateX(-90deg) translateZ(80px);}
    .face-earth, .face-radar{ transform:scale(0.72); }
    .face-ui{ width:120px; gap:8px; }
    .face-city{ height:78px; gap:4px; } .city-bar{ width:9px; }
    .face-grid-net{ gap:14px; }
    .face-brand{ font-size:0.95rem; }
  }

  @media (prefers-reduced-motion: reduce){
    .cube{ animation:none; }
    .live-dot{ animation:none; }
    .earth-globe, .earth-ring, .city-bar, .ui-bar, .ui-dot::after, .face-grid-net span, .radar-sweep{ animation:none !important; }
  }

  /* ============ DIGITAL UNIVERSE ============ */
  .universe{
    background:
      radial-gradient(circle at 50% 0%, rgba(137,101,255,0.16), transparent 45%),
      radial-gradient(circle at 15% 60%, rgba(101,255,207,0.08), transparent 40%),
      radial-gradient(circle at 85% 40%, rgba(255,79,203,0.1), transparent 40%),
      var(--space);
    text-align:center; overflow:hidden;
  }
  .universe .section-label{ justify-content:center; }
  .universe .section-label::after{ display:none; }
  .universe .section-title, .universe .section-sub{ margin-left:auto; margin-right:auto; text-align:center; }

  .globe-stage{
    position:relative; width:min(640px, 90vw); aspect-ratio:1;
    margin:88px auto 30px; isolation:isolate;
  }
  .orbit-ring{
    position:absolute; top:50%; left:50%; border-radius:50%;
    transform:translate(-50%,-50%); border:1px solid rgba(151,125,255,0.22);
    animation:ringSpin linear infinite;
  }
  .ring-1{ width:34%; height:34%; border-color:rgba(101,255,207,0.3); animation-duration:16s; }
  .ring-2{ width:56%; height:56%; animation-duration:26s; animation-direction:reverse; }
  .ring-3{ width:80%; height:80%; border-color:rgba(255,79,203,0.18); animation-duration:38s; }
  @keyframes ringSpin{ to{ transform:translate(-50%,-50%) rotate(360deg); } }

  .orbit-wrap{ position:absolute; inset:0; animation:ringSpin linear infinite; }
  .orbit-wrap.ow-1{ animation-duration:16s; }
  .orbit-wrap.ow-2{ animation-duration:26s; animation-direction:reverse; }
  .node{
    position:absolute; top:50%; left:50%; width:7px; height:7px; border-radius:50%;
    background:var(--cyan); box-shadow:0 0 10px var(--cyan), 0 0 22px var(--cyan);
    animation:nodePulse 2s ease-in-out infinite;
  }
  .ow-1 .node{ transform:translate(-50%,-50%) translateY(-17vmin); }
  .ow-2 .node{ transform:translate(-50%,-50%) translateY(-28vmin); background:var(--violet-light); box-shadow:0 0 10px var(--violet-light); }
  .ow-2 .node.n2{ transform:translate(-50%,-50%) rotate(160deg) translateY(-28vmin); }

  .globe-core{
    position:absolute; top:50%; left:50%; width:34%; height:34%; transform:translate(-50%,-50%);
    border-radius:50%;
    background:
      repeating-linear-gradient(0deg, rgba(101,255,207,0.32) 0 1px, transparent 1px 12px),
      radial-gradient(circle at 35% 30%, rgba(101,255,207,0.6), rgba(30,50,70,0.9) 55%, rgba(8,5,28,1) 100%);
    box-shadow:0 0 60px rgba(101,255,207,0.35), 0 0 120px rgba(137,101,255,0.2), inset -20px -20px 40px rgba(0,0,0,0.6);
    animation:earthSpin 8s linear infinite;
  }

  .link{ position:absolute; top:50%; left:50%; height:2px; width:23%; transform-origin:left center;
    background:linear-gradient(90deg, rgba(151,125,255,0.7), transparent); }
  .link-top{ transform:rotate(-90deg); }
  .link-right{ transform:rotate(0deg); }
  .link-bottom{ transform:rotate(90deg); }
  .link-left{ transform:rotate(180deg); }

  .universe-card{
    position:absolute; width:170px; padding:16px 18px; text-align:left; z-index:2;
    background:rgba(18,12,50,0.5); border:1px solid rgba(151,125,255,0.32); border-radius:16px;
    backdrop-filter:blur(14px); box-shadow:0 16px 40px rgba(20,10,60,0.4), inset 0 1px 0 rgba(255,255,255,0.06);
    animation:cardFloat 5.5s ease-in-out infinite;
  }
  .universe-card .u-icon{
    width:30px; height:30px; border-radius:9px; margin-bottom:10px;
    display:flex; align-items:center; justify-content:center; font-size:0.95rem;
    background:rgba(101,255,207,0.1); border:1px solid rgba(101,255,207,0.35); color:var(--cyan);
  }
  .universe-card h3{ font-size:0.92rem; margin-bottom:4px; letter-spacing:0.01em; }
  .universe-card p{ font-size:0.76rem; color:rgba(241,238,255,0.6); line-height:1.4; }
  .card-top{ top:-6%; left:50%; transform:translate(-50%,0); animation-delay:0s; }
  .card-left{ top:50%; left:-8%; transform:translate(0,-50%); animation-delay:1.3s; }
  .card-right{ top:50%; right:-8%; transform:translate(0,-50%); animation-delay:2.6s; }
  .card-bottom{ bottom:-6%; left:50%; transform:translate(-50%,0); animation-delay:3.9s; }
  @keyframes cardFloat{ 0%,100%{ transform:translate(-50%,0) translateY(0); } 50%{ transform:translate(-50%,0) translateY(-9px); } }
  .card-left, .card-right{ animation-name:cardFloatSide; }
  @keyframes cardFloatSide{ 0%,100%{ transform:translate(0,-50%) translateY(0); } 50%{ transform:translate(0,-50%) translateY(-9px); } }

  @media (max-width:900px){
    .globe-stage{ width:min(360px,80vw); margin:60px auto 250px; }
    .universe-card{ width:150px; padding:12px 14px; }
    .link{ display:none; }
    .card-top{ top:-24%; }
    .card-bottom{ bottom:-24%; }
    .card-left{ left:-26%; }
    .card-right{ right:-26%; }
  }
  @media (max-width:560px){
    .globe-stage{ width:78vw; margin:50px auto 460px; }
    .universe-card{ position:static; width:auto; transform:none !important; margin:0 0 14px; animation:none; }
    .card-top,.card-left,.card-right,.card-bottom{ position:static; }
    .universe-cards-mobile{ display:flex; flex-direction:column; margin-top:30px; }
  }

  .universe-metrics-label{
    margin-top:36px; color:var(--cyan); font-size:0.78rem; letter-spacing:0.14em;
  }
  .metrics-grid{
    display:grid; grid-template-columns:repeat(4,1fr); gap:1px; background:var(--line);
    border:1px solid var(--line); border-radius:18px; overflow:hidden; margin-top:26px;
  }
  .metric-card{ background:rgba(13,8,48,0.6); padding:30px 16px; }
  .metric-value{ display:block; font-family:'Space Grotesk', sans-serif; font-weight:700;
    font-size:clamp(1.5rem,3vw,2rem); color:var(--cyan); text-shadow:0 0 22px rgba(101,255,207,0.4); }
  .metric-label{ display:block; margin-top:8px; font-size:0.78rem; color:rgba(241,238,255,0.6); }
  @media (max-width:700px){ .metrics-grid{ grid-template-columns:repeat(2,1fr); } }

  .timeline{ display:flex; align-items:center; justify-content:center; gap:14px; margin-top:80px; flex-wrap:wrap; }
  .timeline-step{ display:flex; flex-direction:column; align-items:center; gap:12px; width:110px; }
  .timeline-icon{
    width:58px; height:58px; border-radius:50%; display:flex; align-items:center; justify-content:center;
    background:rgba(18,12,50,0.6); border:1px solid rgba(151,125,255,0.35);
    box-shadow:0 0 24px rgba(137,101,255,0.25); font-size:1.3rem; color:var(--cyan);
    animation:ringPulse 3.4s ease-in-out infinite;
  }
  .timeline-step span{ font-family:'IBM Plex Mono', monospace; font-size:0.72rem; letter-spacing:0.1em; color:rgba(241,238,255,0.75); }
  .timeline-arrow{ color:var(--violet-light); font-size:1.1rem; opacity:0.7; }
  @media (max-width:560px){ .timeline-arrow{ transform:rotate(90deg); } .timeline{ flex-direction:column; gap:22px; } }

  .universe-city{
    position:relative; margin-top:110px; height:190px;
    display:flex; align-items:flex-end; justify-content:center; gap:5px;
    padding:0 2vw; opacity:0.65;
  }
  .universe-city span{
    flex:1; max-width:26px;
    background:linear-gradient(180deg, rgba(137,101,255,0.05), rgba(101,255,207,0.35));
    box-shadow:0 0 16px rgba(101,255,207,0.25);
    position:relative; animation:cityFlicker 3s ease-in-out infinite;
  }
  .universe-city span:nth-child(3n){ height:88%; background:linear-gradient(180deg, rgba(255,79,203,0.05), rgba(255,79,203,0.4)); box-shadow:0 0 16px rgba(255,79,203,0.3); }
  .universe-city span:nth-child(4n){ height:55%; }
  .universe-city span:nth-child(5n){ height:100%; background:linear-gradient(180deg, rgba(137,101,255,0.05), rgba(137,101,255,0.45)); box-shadow:0 0 16px rgba(137,101,255,0.3); }
  .universe-city span:nth-child(7n){ height:40%; }
  .universe-city::before{
    content:''; position:absolute; inset:0 0 auto 0; height:2px;
    background:linear-gradient(90deg, transparent, var(--cyan), transparent);
    filter:blur(1px); opacity:0.5; animation:dataStream 4s linear infinite;
  }
  @keyframes dataStream{ 0%{ transform:translateY(-10px); opacity:0; } 20%{ opacity:0.7; } 100%{ transform:translateY(190px); opacity:0; } }

  .reveal{ opacity:0; transform:translateY(24px); transition:opacity .8s ease, transform .8s ease; }
  .reveal.in-view{ opacity:1; transform:translateY(0); }

  @media (prefers-reduced-motion: reduce){
    .orbit-ring, .orbit-wrap, .node, .globe-core, .universe-card, .timeline-icon, .universe-city span, .universe-city::before{ animation:none !important; }
    .reveal{ opacity:1; transform:none; transition:none; }
  }

  /* ============ SSC-BOT MASCOT ============ */
  .ssc-bot{
    position:fixed; top:0; left:0; z-index:70; pointer-events:none;
    width:64px; height:72px;
    transform:translate3d(-120px,-120px,0) rotate(0deg);
    will-change:transform;
  }
  .bot-inner{ position:relative; width:100%; height:100%; animation:botIdleBob 2.6s ease-in-out infinite; }
  .bot-shadow-glow{
    position:absolute; bottom:-2px; left:50%; width:38px; height:9px; transform:translateX(-50%);
    background:radial-gradient(ellipse, rgba(101,255,207,0.4), transparent 72%); filter:blur(3px);
  }

  /* twin antennae */
  .bot-antenna{ position:absolute; top:-11px; width:2px; height:13px; background:var(--violet-light); }
  .bot-antenna.a-l{ left:20px; transform:rotate(-12deg); }
  .bot-antenna.a-r{ right:20px; left:auto; transform:rotate(12deg); }
  .bot-antenna span{
    position:absolute; top:-6px; left:50%; width:6px; height:6px; border-radius:50%;
    background:var(--cyan); box-shadow:0 0 9px var(--cyan); transform:translateX(-50%);
    animation:liveBlink 1.6s infinite;
  }
  .bot-antenna.a-r span{ animation-delay:.4s; }

  /* rounded squircle head with ear-cup style side pieces */
  .bot-head{
    width:46px; height:38px; margin:2px auto 0; border-radius:20px 20px 17px 17px;
    background:linear-gradient(160deg,#F3F1FF,#C4BAF2 55%,#A79BE0);
    box-shadow:0 6px 16px rgba(0,0,0,0.45), inset 0 -5px 7px rgba(0,0,0,0.15), inset 0 3px 4px rgba(255,255,255,0.5);
    position:relative;
  }
  .bot-ear{
    position:absolute; top:50%; width:11px; height:16px; transform:translateY(-50%);
    border-radius:6px; background:linear-gradient(160deg,#D7CFFA,#9C8FD9);
    box-shadow:0 3px 8px rgba(0,0,0,0.35), inset 0 2px 3px rgba(255,255,255,0.4);
  }
  .bot-ear.ear-l{ left:-7px; }
  .bot-ear.ear-r{ right:-7px; }
  .bot-ear span{
    position:absolute; inset:3px; border-radius:4px; background:rgba(11,7,34,0.55);
  }
  .bot-visor{
    position:absolute; inset:7px 6px 8px; border-radius:12px; background:#0B0722;
    box-shadow:inset 0 0 10px rgba(101,255,207,0.15);
    display:flex; align-items:center; justify-content:center; gap:6px; overflow:hidden;
  }
  .bot-eye{
    width:7px; height:10px; border-radius:3px; background:var(--cyan); box-shadow:0 0 9px var(--cyan);
    transition:height .2s ease, width .2s ease, border-radius .2s ease, background .2s ease, box-shadow .2s ease, transform .2s ease;
  }
  .bot-mouth{
    position:absolute; bottom:5px; left:50%; width:9px; height:2px; border-radius:2px;
    background:rgba(101,255,207,0.7); transform:translateX(-50%); transition:all .2s ease;
  }

  /* rounded chunky body with heart badge + tiny arms */
  .bot-body{
    width:32px; height:24px; margin:3px auto 0; border-radius:13px;
    background:linear-gradient(160deg,#E4DFFF,#B0A4E8);
    box-shadow:0 5px 12px rgba(0,0,0,0.35), inset 0 -3px 5px rgba(0,0,0,0.12), inset 0 2px 3px rgba(255,255,255,0.4);
    position:relative;
  }
  .bot-heart{
    position:absolute; top:5px; left:50%; width:8px; height:8px; background:var(--magenta);
    border-radius:50% 50% 50% 0; transform:translateX(-50%) rotate(45deg); box-shadow:0 0 9px var(--magenta); opacity:0.9;
  }
  .bot-arm{
    position:absolute; top:4px; width:6px; height:12px; border-radius:5px;
    background:linear-gradient(160deg,#DBD4FA,#A79BE0); box-shadow:0 2px 5px rgba(0,0,0,0.3);
  }
  .bot-arm.arm-l{ left:-6px; transform:rotate(8deg); }
  .bot-arm.arm-r{ right:-6px; transform:rotate(-8deg); }

  @keyframes botIdleBob{ 0%,100%{ transform:translateY(0); } 50%{ transform:translateY(-4px); } }

  /* state: curious — eyes glance toward motion, ears perk */
  .ssc-bot[data-state="curious"] .bot-eye{ transform:translateX(var(--lookx,0px)); }
  .ssc-bot[data-state="curious"] .bot-ear{ box-shadow:0 3px 8px rgba(0,0,0,0.35), 0 0 8px rgba(101,255,207,0.4); }

  /* state: happy — soft smile, gentle bounce */
  .ssc-bot[data-state="happy"] .bot-eye{ height:6px; border-radius:3px 3px 7px 7px; }
  .ssc-bot[data-state="happy"] .bot-mouth{ width:14px; height:6px; background:transparent; border-bottom:2px solid var(--cyan); border-radius:0 0 9px 9px; }
  .ssc-bot[data-state="happy"] .bot-inner{ animation:botBob 0.55s ease-in-out infinite alternate; }

  /* state: excited — star eyes, quick bounce */
  .ssc-bot[data-state="excited"] .bot-eye{
    width:10px; height:10px; border-radius:2px; background:#FFD24D; box-shadow:0 0 11px #FFD24D;
    clip-path:polygon(50% 0%,63% 35%,100% 35%,70% 57%,82% 100%,50% 76%,18% 100%,30% 57%,0% 35%,37% 35%);
  }
  .ssc-bot[data-state="excited"] .bot-mouth{ width:13px; height:7px; background:transparent; border-bottom:2px solid #FFD24D; border-radius:0 0 9px 9px; }
  .ssc-bot[data-state="excited"] .bot-inner{ animation:botBob 0.28s ease-in-out infinite alternate; }

  /* state: sleepy — half-closed eyes, zzz, slow drift */
  .ssc-bot[data-state="sleepy"] .bot-eye{ height:2px; opacity:0.65; }
  .ssc-bot[data-state="sleepy"] .bot-mouth{ opacity:0.35; }
  .ssc-bot[data-state="sleepy"] .bot-inner{ animation:botDrift 3.2s ease-in-out infinite; }
  .ssc-bot[data-state="sleepy"] .bot-head::after{
    content:'z z'; position:absolute; top:-26px; right:-16px; font-family:'Space Grotesk', sans-serif;
    font-size:0.72rem; color:var(--violet-light); opacity:0.85; animation:zFloat 2.6s ease-in-out infinite;
  }
  @keyframes botDrift{ 0%,100%{ transform:translateY(0) rotate(0deg); } 50%{ transform:translateY(-3px) rotate(-3deg); } }
  @keyframes zFloat{ 0%{ transform:translateY(0); opacity:0; } 30%{ opacity:0.85; } 100%{ transform:translateY(-14px); opacity:0; } }

  /* state: celebrate — spin + big grin */
  .ssc-bot[data-state="celebrate"] .bot-mouth{ width:14px; height:7px; background:transparent; border-bottom:2px solid #FFD24D; border-radius:0 0 10px 10px; }
  .ssc-bot[data-state="celebrate"] .bot-eye{ height:6px; border-radius:3px 3px 7px 7px; background:#FFD24D; box-shadow:0 0 11px #FFD24D; }
  .ssc-bot[data-state="celebrate"] .bot-arm.arm-l{ animation:armWave 0.5s ease-in-out 3; }
  .ssc-bot[data-state="celebrate"] .bot-arm.arm-r{ animation:armWave 0.5s ease-in-out 3 reverse; }
  .ssc-bot[data-state="celebrate"] .bot-inner{ animation:botSpin 0.7s ease-in-out; }
  @keyframes botSpin{ 0%{ transform:rotate(0); } 100%{ transform:rotate(360deg); } }
  @keyframes armWave{ 0%,100%{ transform:rotate(8deg); } 50%{ transform:rotate(-30deg) translateY(-4px); } }

  /* state: listening — ears + eyes glow, gentle head tilt, breathing ring */
  .ssc-bot[data-state="listening"] .bot-ear{ box-shadow:0 3px 8px rgba(0,0,0,0.35), 0 0 14px rgba(101,255,207,0.75); }
  .ssc-bot[data-state="listening"] .bot-eye{ box-shadow:0 0 14px var(--cyan); }
  .ssc-bot[data-state="listening"] .bot-inner{ animation:botListen 1.1s ease-in-out infinite; }
  @keyframes botListen{ 0%,100%{ transform:rotate(0deg) translateY(0); } 50%{ transform:rotate(-3deg) translateY(-3px); } }
  .bot-listen-ring{
    position:absolute; top:-6px; left:50%; width:52px; height:52px; transform:translateX(-50%);
    border:2px solid rgba(101,255,207,0.55); border-radius:50%; opacity:0;
  }
  .ssc-bot[data-state="listening"] .bot-listen-ring{ animation:listenPulse 1.4s ease-out infinite; }
  @keyframes listenPulse{ 0%{ transform:translateX(-50%) scale(0.6); opacity:0.8; } 100%{ transform:translateX(-50%) scale(1.5); opacity:0; } }

  /* state: thinking — eyes drift up, small pause */
  .ssc-bot[data-state="thinking"] .bot-eye{ transform:translateY(-2px); border-radius:50%; width:6px; height:6px; }
  .ssc-bot[data-state="thinking"] .bot-mouth{ width:6px; border-radius:3px; }
  .ssc-bot[data-state="thinking"] .bot-inner{ animation:botThink 1.8s ease-in-out infinite; }
  @keyframes botThink{ 0%,100%{ transform:rotate(0deg); } 50%{ transform:rotate(4deg); } }

  /* state: confused — head tilt + asymmetric blink */
  .ssc-bot[data-state="confused"] .bot-inner{ animation:botConfused 1.4s ease-in-out infinite; }
  .ssc-bot[data-state="confused"] .bot-eye.eye-r{ height:3px; }
  @keyframes botConfused{ 0%,100%{ transform:rotate(0deg); } 50%{ transform:rotate(-9deg); } }

  .bot-wiggle .bot-inner{ animation:botWiggle 1.1s ease-in-out !important; }
  @keyframes botWiggle{
    0%,100%{ transform:rotate(0); } 20%{ transform:rotate(-16deg); } 40%{ transform:rotate(12deg); }
    60%{ transform:rotate(-9deg); } 80%{ transform:rotate(5deg); }
  }

  .bot-confetti{
    position:fixed; width:6px; height:6px; border-radius:50%; z-index:80; pointer-events:none;
    animation:confettiPop 0.9s ease-out forwards;
  }
  @keyframes confettiPop{ to{ transform:translate(var(--dx),var(--dy)) scale(0); opacity:0; } }

  .bot-particle{
    position:fixed; width:5px; height:5px; border-radius:50%; z-index:80; pointer-events:none;
    background:var(--cyan); box-shadow:0 0 10px var(--cyan);
    animation:particleFloat 1.1s ease-out forwards;
  }
  @keyframes particleFloat{ to{ transform:translate(var(--dx),var(--dy)) scale(0); opacity:0; } }

  .bot-trail-dot{
    position:fixed; width:5px; height:5px; border-radius:50%; z-index:65; pointer-events:none;
    background:var(--cyan); box-shadow:0 0 8px var(--cyan);
    animation:trailFade 0.6s ease-out forwards;
  }
  @keyframes trailFade{ to{ transform:scale(0.2); opacity:0; } }

  /* state: greet — wave hello */
  .bot-greet .bot-arm.arm-r{ animation:armWave 0.5s ease-in-out 3; }
  .bot-greet .bot-eye{ height:6px; border-radius:3px 3px 7px 7px; }
  .bot-greet .bot-mouth{ width:14px; height:6px; background:transparent; border-bottom:2px solid var(--cyan); border-radius:0 0 9px 9px; }

  /* speech bubble */
  .bot-bubble{
    position:fixed; top:0; left:0; z-index:75; pointer-events:none;
    max-width:190px; padding:9px 13px; border-radius:14px 14px 14px 4px;
    background:rgba(16,10,44,0.92); border:1px solid rgba(101,255,207,0.4);
    backdrop-filter:blur(10px); box-shadow:0 10px 28px rgba(20,10,60,0.45);
    font-family:'Space Grotesk', sans-serif; font-size:0.8rem; line-height:1.35; color:var(--cream);
    opacity:0; transform:translate3d(-999px,-999px,0) scale(0.82); transform-origin:bottom left;
    transition:opacity .22s ease, transform .22s ease;
  }
  .bot-bubble.show{ opacity:1; transform:translate3d(var(--bx,0),var(--by,0),0) scale(1); }
  @media (max-width:640px){ .bot-bubble{ display:none; } }
  @media (prefers-reduced-motion: reduce){ .bot-bubble{ display:none; } }

  @media (max-width:640px){ .ssc-bot{ display:none; } }
  @media (prefers-reduced-motion: reduce){ .ssc-bot{ display:none; } }

  /* state: peek — half-hidden, only head visible, shy */
  .ssc-bot[data-state="peek"] .bot-body{ opacity:0; }
  .ssc-bot[data-state="peek"] .bot-arm{ opacity:0; }

  /* fall + recover */
  @keyframes botFall{
    0%{ transform:translateY(0) rotate(0deg); }
    45%{ transform:translateY(22px) rotate(18deg); }
    70%{ transform:translateY(4px) rotate(-6deg); }
    100%{ transform:translateY(0) rotate(0deg); }
  }
  .bot-fall .bot-inner{ animation:botFall 0.7s ease-in-out !important; }

  /* arrival portal */
  .bot-portal{
    position:fixed; z-index:69; width:120px; height:120px; border-radius:50%;
    pointer-events:none; opacity:0;
    background:radial-gradient(circle, rgba(101,255,207,0.5), rgba(137,101,255,0.35) 45%, transparent 72%);
    box-shadow:0 0 50px rgba(101,255,207,0.5), 0 0 100px rgba(137,101,255,0.35);
    transform:translate(-50%,-50%) scale(0.2);
  }
  .bot-portal.open{ animation:portalOpen 1.1s cubic-bezier(.2,.9,.3,1) forwards; }
  @keyframes portalOpen{
    0%{ opacity:0; transform:translate(-50%,-50%) scale(0.1) rotate(0deg); }
    35%{ opacity:1; transform:translate(-50%,-50%) scale(1.15) rotate(60deg); }
    70%{ opacity:0.8; transform:translate(-50%,-50%) scale(0.95) rotate(160deg); }
    100%{ opacity:0; transform:translate(-50%,-50%) scale(0.4) rotate(260deg); }
  }
  @media (prefers-reduced-motion: reduce){ .bot-portal{ display:none; } }

  /* data crystal mini-game */
  .data-crystal{
    position:fixed; z-index:78; width:34px; height:34px; cursor:pointer;
    background:linear-gradient(155deg, rgba(255,255,255,0.9), var(--cyan) 55%, var(--violet-light));
    clip-path:polygon(50% 0%, 90% 35%, 74% 100%, 26% 100%, 10% 35%);
    box-shadow:0 0 22px rgba(101,255,207,0.7), 0 0 44px rgba(101,255,207,0.35);
    animation:crystalFloat 1.8s ease-in-out infinite, crystalPop 0.4s ease-out;
    transform-origin:center;
  }
  @keyframes crystalFloat{ 0%,100%{ transform:translateY(0) rotate(-4deg); } 50%{ transform:translateY(-10px) rotate(4deg); } }
  @keyframes crystalPop{ from{ transform:scale(0); opacity:0; } to{ transform:scale(1); opacity:1; } }
  @media (prefers-reduced-motion: reduce){ .data-crystal{ animation:none; } }

  .lead-card{
    margin-top:6px; padding:14px 16px; border-radius:14px;
    background:rgba(101,255,207,0.06); border:1px solid rgba(101,255,207,0.3);
  }
  .lead-card .lc-label{ font-family:'IBM Plex Mono', monospace; font-size:0.62rem; color:var(--cyan); letter-spacing:0.1em; margin-bottom:8px; }
  .lead-card dl{ display:grid; grid-template-columns:auto 1fr; gap:4px 10px; font-size:0.82rem; margin-bottom:12px; }
  .lead-card dt{ color:rgba(241,238,255,0.5); }
  .lead-card dd{ color:var(--cream); font-weight:500; }
  .lead-card .lc-actions{ display:flex; gap:8px; flex-wrap:wrap; }
  .lead-card .lc-btn{
    font-size:0.76rem; padding:8px 14px; border-radius:100px; font-weight:600;
    display:inline-flex; align-items:center; gap:6px;
  }
  .lead-card .lc-btn.primary{ background:var(--wa-green); color:#08051C; }
  .lead-card .lc-btn.secondary{ background:rgba(255,255,255,0.06); color:var(--cream); border:1px solid var(--line); }

  /* ============ SSC-BOT VOICE PANEL ============ */
  .voice-launcher{
    position:fixed; bottom:26px; right:98px; z-index:99;
    display:flex; align-items:center; gap:10px;
    background:rgba(16,10,44,0.85); border:1px solid rgba(101,255,207,0.4);
    backdrop-filter:blur(14px); padding:12px 18px 12px 14px; border-radius:100px;
    box-shadow:0 10px 30px rgba(20,10,60,0.45);
    cursor:pointer; transition:transform .2s ease, border-color .2s ease;
  }
  .voice-launcher:hover{ transform:translateY(-2px); border-color:var(--cyan); }
  .voice-launcher .vl-icon{
    width:34px; height:34px; border-radius:50%; flex-shrink:0;
    background:radial-gradient(circle, rgba(101,255,207,0.25), rgba(101,255,207,0.05));
    border:1px solid rgba(101,255,207,0.5);
    display:flex; align-items:center; justify-content:center; color:var(--cyan);
    animation:ringPulse 2.6s ease-in-out infinite;
  }
  .voice-launcher .vl-text{ font-family:'Space Grotesk', sans-serif; font-size:0.82rem; font-weight:600; color:var(--cream); white-space:nowrap; }
  .voice-launcher .vl-sub{ font-family:'IBM Plex Mono', monospace; font-size:0.6rem; color:rgba(241,238,255,0.5); letter-spacing:0.06em; display:block; }
  @media (max-width:640px){
    .voice-launcher{ right:16px; bottom:98px; left:16px; justify-content:center; }
    .voice-launcher .vl-text{ white-space:normal; }
  }

  .voice-panel{
    position:fixed; inset:0; z-index:200;
    background:rgba(5,3,18,0.72); backdrop-filter:blur(6px);
    display:none; align-items:flex-end; justify-content:center;
  }
  .voice-panel.open{ display:flex; }
  @media (min-width:720px){ .voice-panel{ align-items:center; } }

  .voice-card{
    position:relative; width:min(460px, 94vw); max-height:88vh; overflow:hidden;
    margin-bottom:0; border-radius:26px 26px 0 0;
    background:linear-gradient(165deg, rgba(20,13,54,0.98), rgba(9,6,30,0.98));
    border:1px solid rgba(151,125,255,0.32);
    box-shadow:0 -20px 60px rgba(0,0,0,0.5);
    display:flex; flex-direction:column;
    animation:cardSlideUp .32s cubic-bezier(.2,.9,.3,1);
  }
  @media (min-width:720px){
    .voice-card{ border-radius:26px; margin-bottom:0; max-height:78vh; }
  }
  @keyframes cardSlideUp{ from{ transform:translateY(40px); opacity:0; } to{ transform:translateY(0); opacity:1; } }

  .voice-card-head{
    display:flex; align-items:center; gap:14px; padding:22px 22px 16px;
    border-bottom:1px solid var(--line);
  }
  .voice-avatar{
    width:44px; height:44px; border-radius:50%; flex-shrink:0; position:relative;
    background:linear-gradient(160deg,#F3F1FF,#A79BE0);
    box-shadow:0 0 18px rgba(137,101,255,0.5);
    display:flex; align-items:center; justify-content:center;
  }
  .voice-avatar::after{
    content:''; position:absolute; inset:11px; border-radius:6px; background:#0B0722;
  }
  .voice-avatar .va-dot{
    position:absolute; z-index:1; width:5px; height:7px; border-radius:2px; background:var(--cyan); box-shadow:0 0 6px var(--cyan);
  }
  .voice-avatar .va-dot.va-l{ left:15px; }
  .voice-avatar .va-dot.va-r{ right:15px; }
  .voice-card-head h3{ font-size:1.05rem; }
  .voice-card-head .vc-status{ font-family:'IBM Plex Mono', monospace; font-size:0.66rem; color:var(--cyan); letter-spacing:0.08em; margin-top:2px; }
  .voice-close{
    width:34px; height:34px; border-radius:50%; border:1px solid var(--line);
    display:flex; align-items:center; justify-content:center; color:rgba(241,238,255,0.6);
    background:rgba(255,255,255,0.03); cursor:pointer; flex-shrink:0; font-size:1.1rem;
    transition:border-color .2s ease, color .2s ease;
  }
  .voice-close:hover{ border-color:var(--magenta); color:var(--magenta); }
  .voice-head-actions{ margin-left:auto; display:flex; align-items:center; gap:8px; }
  .voice-mute{
    width:34px; height:34px; border-radius:50%; border:1px solid var(--line);
    display:flex; align-items:center; justify-content:center; color:rgba(241,238,255,0.6);
    background:rgba(255,255,255,0.03); cursor:pointer; flex-shrink:0;
    transition:border-color .2s ease, color .2s ease;
  }
  .voice-mute:hover{ border-color:var(--cyan); color:var(--cyan); }
  .voice-mute[aria-pressed="true"]{ border-color:var(--magenta); color:var(--magenta); }

  .voice-transcript{
    flex:1; overflow-y:auto; padding:18px 22px; display:flex; flex-direction:column; gap:12px;
    min-height:180px; max-height:46vh;
  }
  .vt-msg{ max-width:84%; padding:11px 15px; border-radius:16px; font-size:0.9rem; line-height:1.45; }
  .vt-msg.bot{
    align-self:flex-start; border-radius:16px 16px 16px 4px;
    background:rgba(101,255,207,0.08); border:1px solid rgba(101,255,207,0.25); color:var(--cream);
  }
  .vt-msg.user{
    align-self:flex-end; border-radius:16px 16px 4px 16px;
    background:rgba(137,101,255,0.18); border:1px solid rgba(151,125,255,0.35); color:var(--cream);
  }
  .vt-msg.system{
    align-self:center; font-family:'IBM Plex Mono', monospace; font-size:0.68rem;
    color:rgba(241,238,255,0.45); background:none; border:none; padding:2px 8px;
  }

  .voice-controls{ padding:16px 22px 22px; border-top:1px solid var(--line); }
  .vc-row{ display:flex; align-items:center; gap:10px; }
  .vc-type{
    flex:1; background:rgba(255,255,255,0.04); border:1px solid var(--line); border-radius:100px;
    padding:12px 18px; color:var(--cream); font-family:'Inter', sans-serif; font-size:0.88rem;
    outline:none;
  }
  .vc-type::placeholder{ color:rgba(241,238,255,0.35); }
  .vc-type:focus{ border-color:var(--violet-light); }
  .vc-send, .vc-mic{
    width:46px; height:46px; border-radius:50%; flex-shrink:0; border:none; cursor:pointer;
    display:flex; align-items:center; justify-content:center; transition:transform .15s ease, box-shadow .15s ease;
  }
  .vc-send{ background:var(--violet); color:#fff; box-shadow:0 0 16px rgba(137,101,255,0.4); }
  .vc-send:hover{ transform:translateY(-1px); }
  .vc-mic{ background:var(--wa-green); color:#08051C; box-shadow:0 0 16px rgba(37,211,102,0.4); }
  .vc-mic:hover{ transform:translateY(-1px); }
  .vc-mic.listening{ background:var(--magenta); box-shadow:0 0 0 0 rgba(255,79,203,0.6); animation:micPulse 1.3s infinite; }
  @keyframes micPulse{ 0%{ box-shadow:0 0 0 0 rgba(255,79,203,0.55); } 70%{ box-shadow:0 0 0 16px rgba(255,79,203,0); } 100%{ box-shadow:0 0 0 0 rgba(255,79,203,0); } }
  .vc-mic:disabled{ opacity:0.4; cursor:not-allowed; animation:none; }
  .vc-hint{ margin-top:10px; text-align:center; font-family:'IBM Plex Mono', monospace; font-size:0.64rem; color:rgba(241,238,255,0.4); letter-spacing:0.06em; }
  .vc-suggestions{ display:flex; flex-wrap:wrap; gap:8px; padding:0 22px 14px; }
  .vc-chip{
    font-size:0.74rem; padding:7px 13px; border-radius:100px; border:1px solid var(--line);
    background:rgba(255,255,255,0.03); color:rgba(241,238,255,0.75); cursor:pointer;
    transition:border-color .2s ease, color .2s ease;
  }
  .vc-chip:hover{ border-color:var(--cyan); color:var(--cyan); }

  .highlight-pulse{ animation:highlightPulse 1.6s ease-out; }
  @keyframes highlightPulse{
    0%{ box-shadow:0 0 0 0 rgba(101,255,207,0); }
    20%{ box-shadow:0 0 0 6px rgba(101,255,207,0.18); }
    100%{ box-shadow:0 0 0 0 rgba(101,255,207,0); }
  }

  section{ position:relative; padding:110px 6vw; }
  .section-label{
    font-family:'IBM Plex Mono', monospace; font-size:0.76rem; color:var(--cyan);
    display:flex; align-items:center; gap:14px; margin-bottom:20px;
  }
  .section-label::after{ content:''; height:1px; flex:1; background:var(--line); }
  .section-title{ font-size:clamp(1.8rem,3.4vw,2.6rem); max-width:16ch; margin-bottom:18px; }
  .section-sub{ color:rgba(241,238,255,0.68); max-width:560px; font-size:1.02rem; }

  .services{ background:var(--space-2); border-top:1px solid var(--line); border-bottom:1px solid var(--line); }
  .service-grid{
    display:grid; grid-template-columns:repeat(3,1fr); gap:1px;
    background:var(--line); margin-top:56px; border:1px solid var(--line); border-radius:18px; overflow:hidden;
  }
  .service-card{
    background:rgba(13,8,48,0.6); padding:38px 30px; position:relative; overflow:hidden;
    transition:transform .3s ease, background .3s ease, box-shadow .3s ease;
  }
  .service-card::before{
    content:''; position:absolute; top:0; left:0; right:0; height:2px;
    background:linear-gradient(90deg, var(--violet-light), var(--cyan));
    opacity:0; transition:opacity .3s ease;
  }
  .service-card:hover{
    background:rgba(137,101,255,0.1); transform:translateY(-6px);
    box-shadow:0 20px 40px rgba(76,48,255,0.25), 0 0 0 1px rgba(151,125,255,0.2);
    z-index:1;
  }
  .service-card:hover::before{ opacity:1; }
  .service-num{ font-family:'IBM Plex Mono', monospace; color:var(--violet-light); font-size:0.8rem; margin-bottom:22px; display:block; }
  .service-card h3{ font-size:1.2rem; margin-bottom:12px; }
  .service-card p{ color:rgba(241,238,255,0.62); font-size:0.94rem; }
  @media (max-width:800px){ .service-grid{ grid-template-columns:1fr; } }

  .process-list{ margin-top:56px; }
  .process-item{
    display:grid; grid-template-columns:80px 1fr; gap:28px;
    padding:28px 0; border-top:1px solid var(--line);
  }
  .process-item:last-child{ border-bottom:1px solid var(--line); }
  .process-num{ font-family:'IBM Plex Mono', monospace; color:var(--magenta); font-size:0.95rem; }
  .process-item h3{ font-size:1.15rem; margin-bottom:6px; }
  .process-item p{ color:rgba(241,238,255,0.62); font-size:0.95rem; max-width:520px; }

  .contact{
    background:linear-gradient(155deg, rgba(137,101,255,0.16), transparent 60%), var(--space);
    text-align:center; border-top:1px solid var(--line);
  }
  .contact-inner{ max-width:640px; margin:0 auto; }
  .contact h2{ font-size:clamp(2rem,4.4vw,3.2rem); margin-bottom:18px; }
  .contact p{ color:rgba(241,238,255,0.68); font-size:1.05rem; margin-bottom:36px; }
  .contact-btn{
    display:inline-flex; align-items:center; gap:12px;
    background:var(--wa-green); color:#08051C;
    padding:18px 34px; border-radius:100px; font-weight:600; font-size:1.02rem;
    box-shadow:0 0 30px rgba(37,211,102,0.35);
    transition:transform .2s ease;
  }
  .contact-btn:hover{ transform:translateY(-2px); }
  .contact-note{ margin-top:20px; font-family:'IBM Plex Mono', monospace; font-size:0.76rem; color:rgba(241,238,255,0.45); }

  footer{
    padding:36px 6vw; display:flex; justify-content:space-between; align-items:center;
    border-top:1px solid var(--line); font-size:0.85rem; color:rgba(241,238,255,0.45);
    flex-wrap:wrap; gap:12px;
  }

  .wa-float{
    position:fixed; bottom:26px; right:26px; z-index:100;
    width:60px; height:60px; border-radius:50%;
    background:var(--wa-green); display:flex; align-items:center; justify-content:center;
    box-shadow:0 6px 24px rgba(37,211,102,0.5);
    animation: pulse 2.4s infinite;
  }
  .wa-float svg{ width:30px; height:30px; }
  @keyframes pulse{
    0%{ box-shadow:0 0 0 0 rgba(37,211,102,0.5); }
    70%{ box-shadow:0 0 0 14px rgba(37,211,102,0); }
    100%{ box-shadow:0 0 0 0 rgba(37,211,102,0); }
  }
  @media (prefers-reduced-motion: reduce){ .wa-float{ animation:none; } html{ scroll-behavior:auto; } }

  a:focus-visible, button:focus-visible, .voice-launcher:focus-visible{ outline:2px solid var(--cyan); outline-offset:3px; }
</style>
</head>
<body>

<header>
  <div class="logo">
    <div class="logo-mark">SSC</div>
    <span>GLOBAL SSC</span>
  </div>
  <div class="nav-links">
    <nav>
      <a href="#services">Explore</a>
      <a href="#process">Process</a>
      <a href="#contact">Contact Us</a>
    </nav>
    <a class="nav-cta" href="https://wa.me/916309579202?text=Hi%20Global%20SSC%2C%20I%27d%20like%20to%20build%20a%20website." target="_blank" rel="noopener">WhatsApp</a>
  </div>
</header>

<section class="hero">
  <canvas id="starCanvas"></canvas>

  <div class="hero-skyline" aria-hidden="true">
    <span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span>
  </div>

  <div class="floating-panel panel-a" aria-hidden="true">
    <span class="panel-label mono">UPTIME</span>
    <span class="panel-value">99.98%</span>
  </div>
  <div class="floating-panel panel-b" aria-hidden="true">
    <span class="panel-label mono">SITES LAUNCHED</span>
    <span class="panel-value">240+</span>
  </div>
  <div class="floating-panel panel-c" aria-hidden="true">
    <span class="panel-label mono">RESPONSE TIME</span>
    <span class="panel-value">&lt; 2 HRS</span>
  </div>

  <div class="hero-label"><span class="live-dot"></span> TAKING NEW PROJECTS THIS MONTH</div>

  <h1 class="hero-title">Build your<br><span class="accent">dreams</span> into a website.</h1>

  <div class="cube-stage">
    <div class="cube">
      <div class="face face-front">
        <div class="face-earth"><div class="earth-globe"></div><div class="earth-ring"></div></div>
        <span class="face-tag">EARTH / LIVE</span>
      </div>
      <div class="face face-back">
        <div class="face-city">
          <div class="city-bar" style="--h:38%; --d:0s;"></div>
          <div class="city-bar" style="--h:62%; --d:.3s;"></div>
          <div class="city-bar" style="--h:44%; --d:.6s;"></div>
          <div class="city-bar" style="--h:80%; --d:.15s;"></div>
          <div class="city-bar" style="--h:50%; --d:.45s;"></div>
          <div class="city-bar" style="--h:70%; --d:.75s;"></div>
        </div>
        <span class="face-tag">CITY / 2040</span>
      </div>
      <div class="face face-right">
        <div class="face-ui">
          <div class="ui-bar" style="--w:70%;"></div>
          <div class="ui-bar" style="--w:45%;"></div>
          <div class="ui-bar" style="--w:85%;"></div>
          <div class="ui-dot"></div>
        </div>
        <span class="face-tag">UI / DATA</span>
      </div>
      <div class="face face-left">
        <div class="face-grid-net">
          <span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span>
        </div>
        <span class="face-tag">NET / GRID</span>
      </div>
      <div class="face face-top">
        <div class="face-radar">
          <div class="radar-ring"></div>
          <div class="radar-ring r2"></div>
          <div class="radar-sweep"></div>
        </div>
        <span class="face-tag">SCAN</span>
      </div>
      <div class="face face-bottom">
        <div class="face-brand">GLOBAL<br>SSC</div>
        <span class="face-tag">EST. 2026</span>
      </div>
    </div>
  </div>
  <div class="cube-shadow"></div>

  <p class="hero-sub">Global SSC turns your business, brand, or idea into a website that works as hard as you do — designed, built, and launched end to end.</p>

  <div class="hero-ctas">
    <a class="btn-primary" href="https://wa.me/916309579202?text=Hi%20Global%20SSC%2C%20I%27d%20like%20to%20build%20a%20website." target="_blank" rel="noopener">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2 22l5.25-1.38a9.9 9.9 0 0 0 4.79 1.22h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.01A9.82 9.82 0 0 0 12.04 2zm0 18.1h-.01a8.2 8.2 0 0 1-4.19-1.15l-.3-.18-3.11.82.83-3.03-.2-.31a8.2 8.2 0 0 1-1.26-4.38c0-4.54 3.7-8.24 8.25-8.24 2.2 0 4.27.86 5.83 2.42a8.2 8.2 0 0 1 2.41 5.83c0 4.55-3.7 8.24-8.25 8.24zm4.52-6.17c-.25-.12-1.47-.72-1.69-.81-.23-.08-.4-.12-.56.13-.17.25-.65.81-.79.97-.15.17-.29.19-.54.06-.25-.12-1.05-.39-2-1.23-.74-.66-1.24-1.47-1.39-1.72-.14-.25-.02-.38.11-.51.11-.11.25-.29.37-.43.12-.15.16-.25.25-.42.08-.17.04-.31-.02-.43-.06-.12-.56-1.35-.77-1.85-.2-.48-.41-.42-.56-.43-.14-.01-.31-.01-.48-.01-.17 0-.43.06-.66.31-.23.25-.86.84-.86 2.04 0 1.2.88 2.36 1 2.52.12.17 1.74 2.66 4.22 3.73.59.25 1.05.4 1.41.52.59.19 1.13.16 1.56.1.48-.07 1.47-.6 1.67-1.18.21-.58.21-1.07.15-1.18-.06-.1-.23-.16-.48-.28z"/></svg>
      Chat on WhatsApp
    </a>
    <a class="btn-secondary" href="#services">See what we build</a>
  </div>

  <div class="hero-coords mono">LAT 00.00 / LON 00.00<br>GLOBAL-SSC / WEB-01</div>
</section>

<section class="universe" id="universe">
  <div class="section-label mono reveal">◆ THE GLOBAL SSC DIGITAL UNIVERSE</div>
  <h2 class="section-title reveal">One core. Four disciplines. Infinite reach.</h2>
  <p class="section-sub reveal">Everything we build orbits a single connected system — website, automation, security, and infrastructure, working as one living ecosystem.</p>

  <div class="globe-stage reveal">
    <div class="orbit-ring ring-1"></div>
    <div class="orbit-ring ring-2"></div>
    <div class="orbit-ring ring-3"></div>

    <div class="orbit-wrap ow-1" aria-hidden="true"><div class="node"></div></div>
    <div class="orbit-wrap ow-2" aria-hidden="true"><div class="node"></div><div class="node n2"></div></div>

    <div class="globe-core" aria-hidden="true"></div>

    <div class="link link-top"></div>
    <div class="link link-right"></div>
    <div class="link link-bottom"></div>
    <div class="link link-left"></div>

    <div class="universe-card card-top">
      <div class="u-icon">◆</div>
      <h3>AI &amp; Automation</h3>
      <p>Intelligent systems that automate and scale your business.</p>
    </div>
    <div class="universe-card card-left">
      <div class="u-icon">◧</div>
      <h3>Website</h3>
      <p>Stunning, fast, responsive sites that represent your brand.</p>
    </div>
    <div class="universe-card card-right">
      <div class="u-icon">◆</div>
      <h3>Cybersecurity</h3>
      <p>Secure your systems, protect data, stay threat-free.</p>
    </div>
    <div class="universe-card card-bottom">
      <div class="u-icon">◧</div>
      <h3>Cloud &amp; Software</h3>
      <p>Scalable infrastructure and powerful software solutions.</p>
    </div>
  </div>

  <div class="universe-metrics-label mono reveal">WE BUILD DIGITAL SYSTEMS</div>
  <div class="metrics-grid reveal">
    <div class="metric-card"><span class="metric-value">240+</span><span class="metric-label">Websites Launched</span></div>
    <div class="metric-card"><span class="metric-value">50+</span><span class="metric-label">Business Solutions</span></div>
    <div class="metric-card"><span class="metric-value">99.98%</span><span class="metric-label">Uptime Delivered</span></div>
    <div class="metric-card"><span class="metric-value">&lt; 2 HRS</span><span class="metric-label">Avg. Response Time</span></div>
  </div>

  <div class="timeline reveal">
    <div class="timeline-step"><div class="timeline-icon">✦</div><span>IDEA</span></div>
    <div class="timeline-arrow">→</div>
    <div class="timeline-step"><div class="timeline-icon">✎</div><span>DESIGN</span></div>
    <div class="timeline-arrow">→</div>
    <div class="timeline-step"><div class="timeline-icon">&lt;/&gt;</div><span>DEVELOPMENT</span></div>
    <div class="timeline-arrow">→</div>
    <div class="timeline-step"><div class="timeline-icon">▲</div><span>LAUNCH</span></div>
  </div>

  <div class="universe-city" aria-hidden="true">
    <span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span>
  </div>
</section>

<div class="bot-portal" id="botPortal" aria-hidden="true"></div>

<div class="ssc-bot" id="sscBot" aria-hidden="true">
  <div class="bot-inner">
    <div class="bot-shadow-glow"></div>
    <div class="bot-listen-ring"></div>
    <div class="bot-antenna a-l"><span></span></div>
    <div class="bot-antenna a-r"><span></span></div>
    <div class="bot-head">
      <div class="bot-ear ear-l"><span></span></div>
      <div class="bot-ear ear-r"><span></span></div>
      <div class="bot-visor">
        <span class="bot-eye eye-l"></span>
        <span class="bot-eye eye-r"></span>
        <span class="bot-mouth"></span>
      </div>
    </div>
    <div class="bot-body">
      <span class="bot-heart"></span>
      <span class="bot-arm arm-l"></span>
      <span class="bot-arm arm-r"></span>
    </div>
  </div>
</div>

<div class="bot-bubble" id="botBubble" aria-live="polite"><span id="botBubbleText"></span></div>

<button class="voice-launcher" id="voiceLauncher" type="button" aria-haspopup="dialog" aria-controls="voicePanel">
  <span class="vl-icon" aria-hidden="true">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 14a3 3 0 0 0 3-3V6a3 3 0 0 0-6 0v5a3 3 0 0 0 3 3zm5-3a5 5 0 0 1-10 0H5a7 7 0 0 0 6 6.93V21h2v-3.07A7 7 0 0 0 19 11h-2z"/></svg>
  </span>
  <span>
    <span class="vl-text">Talk to SSC-BOT</span>
    <span class="vl-sub mono">VOICE ASSISTANT</span>
  </span>
</button>

<div class="voice-panel" id="voicePanel" role="dialog" aria-modal="true" aria-labelledby="voiceTitle">
  <div class="voice-card">
    <div class="voice-card-head">
      <div class="voice-avatar" aria-hidden="true"><span class="va-dot va-l"></span><span class="va-dot va-r"></span></div>
      <div>
        <h3 id="voiceTitle">SSC-BOT</h3>
        <div class="vc-status" id="vcStatus">READY</div>
      </div>
      <div class="voice-head-actions">
        <button class="voice-mute" id="voiceMute" type="button" aria-pressed="false" aria-label="Mute SSC-BOT's voice">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" id="voiceMuteIcon"><path d="M3 10v4h4l5 5V5L7 10H3zm13.5 2A4.5 4.5 0 0 0 14 7.97v8.05c1.48-.73 2.5-2.25 2.5-4.02zM14 3.23v2.06c2.89.86 5 3.54 5 6.71s-2.11 5.85-5 6.71v2.06c4.01-.91 7-4.49 7-8.77s-2.99-7.86-7-8.77z"/></svg>
        </button>
        <button class="voice-close" id="voiceClose" type="button" aria-label="Close SSC-BOT">✕</button>
      </div>
    </div>
    <div class="voice-transcript" id="voiceTranscript" aria-live="polite"></div>
    <div class="vc-suggestions" id="vcSuggestions">
      <span class="vc-chip" data-q="Can you build a website for my business?">Build my website</span>
      <span class="vc-chip" data-q="What about cybersecurity?">Cybersecurity</span>
      <span class="vc-chip" data-q="How much does it cost?">Pricing</span>
      <span class="vc-chip" data-q="I want to start a project">Start a project</span>
    </div>
    <div class="voice-controls">
      <div class="vc-row">
        <input class="vc-type" id="vcInput" type="text" placeholder="Type your question…" aria-label="Type your question">
        <button class="vc-mic" id="vcMic" type="button" aria-label="Speak to SSC-BOT">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 14a3 3 0 0 0 3-3V6a3 3 0 0 0-6 0v5a3 3 0 0 0 3 3zm5-3a5 5 0 0 1-10 0H5a7 7 0 0 0 6 6.93V21h2v-3.07A7 7 0 0 0 19 11h-2z"/></svg>
        </button>
        <button class="vc-send" id="vcSend" type="button" aria-label="Send">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M2 21l21-9L2 3v7l15 2-15 2z"/></svg>
        </button>
      </div>
      <div class="vc-hint mono" id="vcHint">TAP THE MIC AND SPEAK, OR TYPE YOUR QUESTION</div>
    </div>
  </div>
</div>

<section class="services" id="services">
  <div class="section-label">01 — WHAT WE BUILD</div>
  <h2 class="section-title">From a rough idea to a live website.</h2>
  <p class="section-sub">Whether you're starting a business, launching a brand, or replacing a site that isn't working — we handle the whole build.</p>

  <div class="service-grid">
    <div class="service-card"><span class="service-num mono">FIG. 01</span><h3>Business Websites</h3><p>Clean, fast sites for local businesses, shops, and services — built to bring in calls and customers.</p></div>
    <div class="service-card"><span class="service-num mono">FIG. 02</span><h3>Landing Pages</h3><p>Focused, high-converting pages for a single product, launch, or campaign.</p></div>
    <div class="service-card"><span class="service-num mono">FIG. 03</span><h3>E-Commerce</h3><p>Online stores that make it simple for you to list products and simple for customers to buy.</p></div>
    <div class="service-card"><span class="service-num mono">FIG. 04</span><h3>Portfolio Sites</h3><p>A polished home for your work — freelancers, creatives, and consultants.</p></div>
    <div class="service-card"><span class="service-num mono">FIG. 05</span><h3>Redesigns</h3><p>Already have a site that isn't working? We'll rebuild it around what your visitors actually need.</p></div>
    <div class="service-card"><span class="service-num mono">FIG. 06</span><h3>Ongoing Support</h3><p>Updates, fixes, and new pages as your business grows — one WhatsApp message away.</p></div>
  </div>
</section>

<section id="process">
  <div class="section-label">02 — HOW IT WORKS</div>
  <h2 class="section-title">One conversation is all it takes to start.</h2>
  <p class="section-sub">No long forms, no waiting on email. Message us on WhatsApp and we take it from there.</p>

  <div class="process-list">
    <div class="process-item"><span class="process-num">01</span><div><h3>Tell us your idea</h3><p>Message us on WhatsApp with what you're building and what the site needs to do.</p></div></div>
    <div class="process-item"><span class="process-num">02</span><div><h3>We plan it out</h3><p>We come back with a simple plan, timeline, and price — no jargon, no surprises.</p></div></div>
    <div class="process-item"><span class="process-num">03</span><div><h3>We design and build</h3><p>You see drafts along the way and give feedback directly, right in the chat.</p></div></div>
    <div class="process-item"><span class="process-num">04</span><div><h3>We launch it</h3><p>Your site goes live, and we stay reachable for anything you need after.</p></div></div>
  </div>
</section>

<section class="contact" id="contact">
  <div class="contact-inner">
    <h2>Have an idea? Let's build it.</h2>
    <p>Message Global SSC on WhatsApp and tell us what you're dreaming up.</p>
    <a class="contact-btn" href="https://wa.me/916309579202?text=Hi%20Global%20SSC%2C%20I%27d%20like%20to%20build%20a%20website." target="_blank" rel="noopener">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2 22l5.25-1.38a9.9 9.9 0 0 0 4.79 1.22h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.01A9.82 9.82 0 0 0 12.04 2zm0 18.1h-.01a8.2 8.2 0 0 1-4.19-1.15l-.3-.18-3.11.82.83-3.03-.2-.31a8.2 8.2 0 0 1-1.26-4.38c0-4.54 3.7-8.24 8.25-8.24 2.2 0 4.27.86 5.83 2.42a8.2 8.2 0 0 1 2.41 5.83c0 4.55-3.7 8.24-8.25 8.24zm4.52-6.17c-.25-.12-1.47-.72-1.69-.81-.23-.08-.4-.12-.56.13-.17.25-.65.81-.79.97-.15.17-.29.19-.54.06-.25-.12-1.05-.39-2-1.23-.74-.66-1.24-1.47-1.39-1.72-.14-.25-.02-.38.11-.51.11-.11.25-.29.37-.43.12-.15.16-.25.25-.42.08-.17.04-.31-.02-.43-.06-.12-.56-1.35-.77-1.85-.2-.48-.41-.42-.56-.43-.14-.01-.31-.01-.48-.01-.17 0-.43.06-.66.31-.23.25-.86.84-.86 2.04 0 1.2.88 2.36 1 2.52.12.17 1.74 2.66 4.22 3.73.59.25 1.05.4 1.41.52.59.19 1.13.16 1.56.1.48-.07 1.47-.6 1.67-1.18.21-.58.21-1.07.15-1.18-.06-.1-.23-.16-.48-.28z"/></svg>
      +91 63095 79202
    </a>
    <p class="contact-note">TAP TO OPEN WHATSAPP — REPLIES USUALLY WITHIN A FEW HOURS</p>
  </div>
</section>

<footer>
  <span>© 2026 Global SSC. Build your dreams and idea into a website.</span>
  <span class="mono">GLOBAL-SSC / IND</span>
</footer>

<a class="wa-float" href="https://wa.me/916309579202?text=Hi%20Global%20SSC%2C%20I%27d%20like%20to%20build%20a%20website." target="_blank" rel="noopener" aria-label="Chat with Global SSC on WhatsApp">
  <svg viewBox="0 0 24 24" fill="#08051C"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2 22l5.25-1.38a9.9 9.9 0 0 0 4.79 1.22h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.01A9.82 9.82 0 0 0 12.04 2zm0 18.1h-.01a8.2 8.2 0 0 1-4.19-1.15l-.3-.18-3.11.82.83-3.03-.2-.31a8.2 8.2 0 0 1-1.26-4.38c0-4.54 3.7-8.24 8.25-8.24 2.2 0 4.27.86 5.83 2.42a8.2 8.2 0 0 1 2.41 5.83c0 4.55-3.7 8.24-8.25 8.24zm4.52-6.17c-.25-.12-1.47-.72-1.69-.81-.23-.08-.4-.12-.56.13-.17.25-.65.81-.79.97-.15.17-.29.19-.54.06-.25-.12-1.05-.39-2-1.23-.74-.66-1.24-1.47-1.39-1.72-.14-.25-.02-.38.11-.51.11-.11.25-.29.37-.43.12-.15.16-.25.25-.42.08-.17.04-.31-.02-.43-.06-.12-.56-1.35-.77-1.85-.2-.48-.41-.42-.56-.43-.14-.01-.31-.01-.48-.01-.17 0-.43.06-.66.31-.23.25-.86.84-.86 2.04 0 1.2.88 2.36 1 2.52.12.17 1.74 2.66 4.22 3.73.59.25 1.05.4 1.41.52.59.19 1.13.16 1.56.1.48-.07 1.47-.6 1.67-1.18.21-.58.21-1.07.15-1.18-.06-.1-.23-.16-.48-.28z"/></svg>
</a>

<script>
  const canvas = document.getElementById('starCanvas');
  const ctx = canvas.getContext('2d');
  let stars = [];

  function resize(){
    canvas.width = canvas.offsetWidth;
    canvas.height = canvas.offsetHeight;
    const count = Math.floor((canvas.width * canvas.height) / 6000);
    stars = Array.from({length: count}, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.3 + 0.2,
      speed: Math.random() * 0.15 + 0.02,
      twinkle: Math.random() * Math.PI * 2
    }));
  }

  function draw(){
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    for(const s of stars){
      s.twinkle += 0.02;
      const alpha = 0.4 + Math.sin(s.twinkle) * 0.4;
      ctx.beginPath();
      ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(220,210,255,${Math.max(alpha,0.1)})`;
      ctx.fill();
      s.y -= s.speed;
      if(s.y < 0) s.y = canvas.height;
    }
    requestAnimationFrame(draw);
  }

  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  window.addEventListener('resize', resize);
  resize();
  if(!reduceMotion){ draw(); } else {
    for(const s of stars){
      ctx.beginPath(); ctx.arc(s.x, s.y, s.r, 0, Math.PI*2);
      ctx.fillStyle = 'rgba(220,210,255,0.4)'; ctx.fill();
    }
  }

  /* ---- scroll reveal for Digital Universe section ---- */
  const revealEls = document.querySelectorAll('.reveal');
  if('IntersectionObserver' in window && !reduceMotion){
    const io = new IntersectionObserver((entries)=>{
      entries.forEach(entry=>{
        if(entry.isIntersecting){ entry.target.classList.add('in-view'); io.unobserve(entry.target); }
      });
    }, { threshold:0.18 });
    revealEls.forEach(el=>io.observe(el));
  } else {
    revealEls.forEach(el=>el.classList.add('in-view'));
  }

  /* ============================================================
     SSC-BOT: living mascot + voice assistant
     ============================================================ */
  (function(){
    const bot = document.getElementById('sscBot');
    const bubble = document.getElementById('botBubble');
    const bubbleText = document.getElementById('botBubbleText');
    const mascotEnabled = bot && !reduceMotion && window.innerWidth >= 640;

    /* ---------- knowledge base (Global SSC's actual services) ---------- */
    const WA_LINK = 'https://wa.me/916309579202?text=Hi%20Global%20SSC%2C%20I%27d%20like%20to%20build%20a%20website.';
    const KB = [
      {
        id: 'business-site',
        pattern: /\b(business website|site for my business|company website|my business|local business|shop website|small business)\b/i,
        target: '#services',
        answer: "Yes! Building business websites is exactly what we do. 😎 Tell me what your business does, and we'll figure out the pages and features that'll bring in customers."
      },
      {
        id: 'landing-page',
        pattern: /\b(landing page|single page|campaign page|product launch page)\b/i,
        target: '#services',
        answer: "Landing pages are one of our specialties — focused, fast, and built to convert for a single product or campaign."
      },
      {
        id: 'ecommerce',
        pattern: /\b(ecommerce|e-commerce|online store|sell products online|shop online|shopping cart)\b/i,
        target: '#services',
        answer: "We build online stores too! Simple for you to manage, simple for customers to check out."
      },
      {
        id: 'portfolio',
        pattern: /\b(portfolio|freelance|freelancer|showcase my work|creative site)\b/i,
        target: '#services',
        answer: "A portfolio site is a great way to show off your work — clean, polished, and all about you."
      },
      {
        id: 'redesign',
        pattern: /\b(redesign|already have a site|old website|revamp|rebuild my site|website isn'?t working)\b/i,
        target: '#services',
        answer: "We can absolutely rebuild it! We'll look at what isn't working and redesign it around what your visitors actually need."
      },
      {
        id: 'support',
        pattern: /\b(support|maintenance|updates|ongoing help|fix my site|add a page)\b/i,
        target: '#services',
        answer: "We stick around after launch too — updates, fixes, and new pages, one WhatsApp message away."
      },
      {
        id: 'ai',
        pattern: /\b(ai|artificial intelligence|automation|automate|chatbot|ai integration)\b/i,
        target: '#universe',
        answer: "Ooh, AI is one of my favorite topics! 🤖 We build intelligent automation systems that help your business run smoother and scale faster."
      },
      {
        id: 'cybersecurity',
        pattern: /\b(cyber ?security|security|secure my (site|data)|hack|hackers|protect (data|my site))\b/i,
        target: '#universe',
        answer: "Hackers? No chance. 😄 We handle cybersecurity — securing your systems and protecting your data so you can stay threat-free."
      },
      {
        id: 'cloud-software',
        pattern: /\b(cloud|infrastructure|software development|custom software|scalable|hosting)\b/i,
        target: '#universe',
        answer: "That falls under our Cloud & Software work — scalable infrastructure and custom software built around what your business needs."
      },
      {
        id: 'pricing',
        pattern: /\b(price|pricing|cost|how much|budget|quote|expensive|cheap)\b/i,
        target: '#contact',
        answer: "Pricing depends on what you need, so we don't like guessing numbers out loud. Message us on WhatsApp with your idea and we'll come back with a clear, no-surprises quote."
      },
      {
        id: 'process',
        pattern: /\b(how does it work|process|how long|timeline|how do (we|i) start)\b/i,
        target: '#process',
        answer: "It's simple: tell us your idea on WhatsApp, we plan it out with you, we design and build it with your feedback, then we launch it. That's it!"
      },
      {
        id: 'start-project',
        pattern: /\b(start a project|start my project|get started|begin|let'?s build|ready to build)\b/i,
        target: '#contact',
        answer: "Yayyy, let's build something amazing! 🚀 I'll pull up our contact section — just message us on WhatsApp with your idea.",
        cta: true
      },
      {
        id: 'contact',
        pattern: /\b(contact|reach you|whatsapp|phone number|talk to (a )?human|talk to someone)\b/i,
        target: '#contact',
        answer: "Of course! You can reach the Global SSC team directly on WhatsApp — I'll open that up for you.",
        cta: true
      },
      {
        id: 'about',
        pattern: /\b(who are you|what is global ssc|about (global )?ssc|what do you do|what does ssc stand for)\b/i,
        target: '#universe',
        answer: "Global SSC builds websites, AI automation, cybersecurity, and cloud & software solutions — all as one connected digital system. I'm SSC-BOT, your guide around here! 💙"
      },
      {
        id: 'services-nav',
        pattern: /\b(show me your services|what services|what do you (build|offer)|services)\b/i,
        target: '#services',
        answer: "Here's everything we build — business websites, landing pages, e-commerce, portfolios, redesigns, and ongoing support."
      }
    ];

    const FALLBACK = "Hmm… that's a tricky one! 🤔 I don't want to make something up. Let me help you find the right answer — message the Global SSC team directly on WhatsApp.";

    // playful, food/venue-style openers for a detected business — cosmetic only, no invented facts
    const BIZ_FLAVOR = {
      restaurant: 'Ooooh, restaurant website! 🍕 Let\u2019s build something delicious!',
      cafe: 'Ooooh, a cafe website! ☕ Let\u2019s brew up something great!',
      salon: 'A salon website — let\u2019s make it look as polished as your work! 💇',
      gym: 'A gym website — let\u2019s build something that hits as hard as leg day! 💪',
      clinic: 'A clinic website — clean, trustworthy, and easy for patients to book. 🩺',
      'real estate': 'Real estate — let\u2019s showcase those listings beautifully! 🏠',
      default: 'Ooooh, nice idea! 😎 Let\u2019s figure out exactly what that website needs.'
    };
    // "website for my X" / "site for my X" — detects a business type Global SSC hasn't been told about yet
    const BIZ_DETECT = /\b(?:website|site)\s+for\s+(?:my|a|our)\s+([a-z][a-z\s]{2,28}?)(?:\s+business)?[.,!?]?$/i;

    function detectBusinessType(text){
      const m = text.match(BIZ_DETECT);
      if(!m) return null;
      const raw = m[1].trim().toLowerCase();
      const flavor = BIZ_FLAVOR[raw] || BIZ_FLAVOR.default;
      return { businessType: raw, flavor };
    }

    function matchKB(text){
      for(const entry of KB){
        if(entry.pattern.test(text)) return entry;
      }
      return null;
    }

    function navigateTo(selector){
      const el = document.querySelector(selector);
      if(!el) return;
      el.scrollIntoView({ behavior: reduceMotion ? 'auto' : 'smooth', block: 'start' });
      el.classList.add('highlight-pulse');
      setTimeout(()=> el.classList.remove('highlight-pulse'), 1700);
      flyBotTo(el);
    }

    // moves the mascot toward a given element, like it's physically guiding the visitor there
    function flyBotTo(el){
      if(!mascotEnabled || !el) return;
      const rect = el.getBoundingClientRect();
      idleTarget = {
        x: Math.min(window.innerWidth - 60, Math.max(60, rect.left + rect.width / 2 + 60)),
        y: Math.min(window.innerHeight - 60, Math.max(60, rect.top + 40))
      };
      mouse = null;
    }

    /* ---------- voice (speech synthesis) ---------- */
    let voiceMuted = false;
    let chosenVoice = null;
    function loadVoice(){
      if(!('speechSynthesis' in window)) return;
      const voices = window.speechSynthesis.getVoices();
      if(!voices.length) return;
      chosenVoice = voices.find(v => /female|girl|child|samantha|victoria|zira|karen/i.test(v.name))
        || voices.find(v => v.lang && v.lang.startsWith('en'))
        || voices[0];
    }
    if('speechSynthesis' in window){
      loadVoice();
      window.speechSynthesis.onvoiceschanged = loadVoice;
    }
    function speak(text, onEnd){
      if(voiceMuted || !('speechSynthesis' in window)){ if(onEnd) onEnd(); return; }
      try{
        const clean = text.replace(/[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}\u{2190}-\u{21FF}]/gu, '').trim();
        if(!clean){ if(onEnd) onEnd(); return; }
        window.speechSynthesis.cancel();
        const u = new SpeechSynthesisUtterance(clean);
        u.pitch = 1.75;
        u.rate = 1.05;
        u.volume = 0.85;
        if(chosenVoice) u.voice = chosenVoice;
        if(onEnd) u.onend = onEnd;
        window.speechSynthesis.speak(u);
      }catch(err){ if(onEnd) onEnd(); }
    }

    /* ---------- mascot speech bubble (only on wide screens) ---------- */
    let pos = { x: -80, y: window.innerHeight * 0.3 };
    let idleTarget = pickIdleTarget();
    let mouse = null;
    let lastActivity = Date.now();
    let curiousTimer = null, scrollTimer = null, celebrateTimer = null, bubbleHideTimer = null;
    let inactiveNoticeGiven = false;
    const lastSaid = {};
    let pendingGreeting = true;
    let botBusy = false; // true while answering a voice/text question

    function pickIdleTarget(){
      const m = 90;
      return {
        x: m + Math.random() * (window.innerWidth - m * 2),
        y: m + Math.random() * (window.innerHeight - m * 2)
      };
    }

    function setState(s){
      if(!mascotEnabled) return;
      if(bot.dataset.state === 'celebrate' && s !== 'celebrate') return;
      bot.dataset.state = s;
    }

    function showBubble(text, duration){
      if(!mascotEnabled) return;
      bubbleText.textContent = text;
      bubble.classList.add('show');
      clearTimeout(bubbleHideTimer);
      bubbleHideTimer = setTimeout(()=> bubble.classList.remove('show'), duration || (1800 + text.length * 35));
    }
    function positionBubble(){
      if(!mascotEnabled) return;
      bubble.style.setProperty('--bx', (pos.x - 30) + 'px');
      bubble.style.setProperty('--by', (pos.y - 58) + 'px');
    }
    function say(text, opts){
      opts = opts || {};
      showBubble(text, opts.duration);
      if(opts.speak !== false) speak(text, opts.onEnd);
      else if(opts.onEnd) opts.onEnd();
    }
    function trySay(key, text, minGapMs, opts){
      const now = Date.now();
      if(lastSaid[key] && now - lastSaid[key] < minGapMs) return;
      lastSaid[key] = now;
      say(text, opts);
    }

    // per-area guide lines — spoken + bot flies toward the section when a visitor picks it
    const AREA_GUIDE = {
      website:  { selector: '#services', line: 'Right this way! 🌐 Let\u2019s look at what we build.' },
      ai:       { selector: '#universe', line: 'Ooh, AI is my favorite corner of the universe! 🤖✨' },
      software: { selector: '#universe', line: 'Cloud & software, coming right up! 💻' },
      security: { selector: '#universe', line: 'Don\u2019t worry! I\u2019ll protect the digital kingdom! 🛡️' },
      project:  { selector: '#contact',  line: 'Yessss! PROJECT MODE ACTIVATED! 🚀', cta:true }
    };
    function guideToArea(key){
      const area = AREA_GUIDE[key];
      if(!area) return;
      if(area.cta) celebrate(area.line); else { setState('excited'); say(area.line, { duration: 2600 }); setTimeout(()=>{ if(!botBusy) setState('idle'); }, 1600); }
      navigateTo(area.selector);
    }

    function greet(){
      if(!mascotEnabled) return;
      const portal = document.getElementById('botPortal');
      if(portal){
        portal.style.left = pos.x + 'px';
        portal.style.top = pos.y + 'px';
        portal.classList.add('open');
        setTimeout(()=> portal.remove(), 1200);
      }
      setState('happy');
      bot.classList.add('bot-greet');
      say('Hiiii! 👋 Hehe… welcome to GLOBAL SSC!', { duration: 2600, onEnd: ()=>{
        setTimeout(()=> say('Are you looking for something awesome? ✨', { duration: 2800 }), 250);
      }});
      setTimeout(()=>{ bot.classList.remove('bot-greet'); setState('idle'); }, 1500);
    }
    setTimeout(()=>{ if(pendingGreeting){ pendingGreeting = false; greet(); } }, 1400);
    ['pointerdown','keydown'].forEach(evt=>{
      window.addEventListener(evt, ()=>{
        if(pendingGreeting){ pendingGreeting = false; greet(); }
        if('speechSynthesis' in window) window.speechSynthesis.resume();
      }, { once:true, passive:true });
    });

    if(mascotEnabled){
      let stopTimer = null;
      let wasAway = false;

      window.addEventListener('mousemove', (e)=>{
        mouse = { x: e.clientX, y: e.clientY };
        const idleForBefore = Date.now() - lastActivity;
        lastActivity = Date.now();
        inactiveNoticeGiven = false;
        if(wasAway || idleForBefore > 9000){
          wasAway = false;
          if(!botBusy){ setState('excited'); trySay('return', 'You\u2019re back! I knew you liked me! Hehe! 😎', 15000); setTimeout(()=>{ if(!botBusy) setState('idle'); }, 1800); }
        } else if(!botBusy && bot.dataset.state !== 'excited' && bot.dataset.state !== 'celebrate'){
          setState('curious');
          trySay('cursor', 'Ooooh… I see you! 👀', 12000, { speak:false });
        }
        clearTimeout(curiousTimer);
        curiousTimer = setTimeout(()=>{ if(bot.dataset.state === 'curious') setState('idle'); }, 1100);

        // detect a visitor who stops moving mid-session (mouse present, but still for a while)
        clearTimeout(stopTimer);
        stopTimer = setTimeout(()=>{
          if(!botBusy && Date.now() - lastActivity >= 4000 && Date.now() - lastActivity < 6500){
            trySay('stopped', 'Umm… did we break the internet? 😂', 25000);
          }
        }, 4200);
      }, { passive:true });

      window.addEventListener('scroll', ()=>{
        lastActivity = Date.now();
        inactiveNoticeGiven = false;
        wasAway = false;
        if(!botBusy && bot.dataset.state !== 'excited' && bot.dataset.state !== 'celebrate'){
          setState('happy');
          trySay('scroll', 'Whoa! You\u2019re exploring! 🚀', 14000, { speak:false });
          clearTimeout(scrollTimer);
          scrollTimer = setTimeout(()=>{ if(bot.dataset.state === 'happy') setState('idle'); }, 900);
        }
      }, { passive:true });

      document.addEventListener('visibilitychange', ()=>{
        if(document.hidden){ wasAway = true; }
      });

      document.querySelectorAll('.service-card, .universe-card').forEach((el)=>{
        el.addEventListener('mouseenter', ()=>{
          lastActivity = Date.now();
          inactiveNoticeGiven = false;
          if(!botBusy){
            setState('excited');
            trySay('hover', 'Ooooh! You found something! Hehe! 😄', 9000);
          }
        });
        el.addEventListener('mouseleave', ()=>{ if(!botBusy) setState('idle'); });
      });

      // universe cards double as the site's interactive "areas" — click one and SSC-BOT flies over
      const AREA_BY_HEADING = { 'website': 'website', 'ai & automation': 'ai', 'cloud & software': 'software', 'cybersecurity': 'security' };
      document.querySelectorAll('.universe-card').forEach((el)=>{
        const heading = (el.querySelector('h3')?.textContent || '').trim().toLowerCase();
        const key = AREA_BY_HEADING[heading];
        if(!key) return;
        el.style.cursor = 'pointer';
        el.addEventListener('click', ()=>{ lastActivity = Date.now(); guideToArea(key); });
      });
    }

    function celebrate(msg){
      if(mascotEnabled){
        setState('celebrate');
        burstConfetti();
        say(msg, { duration: 2600, speak:false });
        clearTimeout(celebrateTimer);
        celebrateTimer = setTimeout(()=>{ if(!botBusy) setState('idle'); }, 1600);
      }
    }

    document.querySelectorAll('.btn-primary, .contact-btn').forEach((el)=>{
      el.addEventListener('click', ()=>{ lastActivity = Date.now(); celebrate('Yayyy! Let\u2019s build something amazing! 🚀'); });
    });
    document.querySelectorAll('.nav-cta, .wa-float').forEach((el)=>{
      el.addEventListener('click', ()=>{ lastActivity = Date.now(); celebrate('Hehe! I\u2019m ready! What are we building? 🤖✨'); });
    });

    function burstConfetti(){
      if(!mascotEnabled) return;
      const rect = bot.getBoundingClientRect();
      const colors = ['#65FFCF', '#A994FF', '#FF4FCB'];
      for(let i = 0; i < 14; i++){
        const p = document.createElement('div');
        p.className = 'bot-confetti';
        const angle = Math.random() * Math.PI * 2;
        const dist = 40 + Math.random() * 70;
        p.style.setProperty('--dx', Math.cos(angle) * dist + 'px');
        p.style.setProperty('--dy', Math.sin(angle) * dist + 'px');
        p.style.left = (rect.left + rect.width / 2) + 'px';
        p.style.top = (rect.top + rect.height / 2) + 'px';
        p.style.background = colors[i % colors.length];
        document.body.appendChild(p);
        setTimeout(()=> p.remove(), 950);
      }
    }
    function burstParticles(){
      if(!mascotEnabled) return;
      const rect = bot.getBoundingClientRect();
      for(let i = 0; i < 8; i++){
        const p = document.createElement('div');
        p.className = 'bot-particle';
        const angle = Math.random() * Math.PI * 2;
        const dist = 26 + Math.random() * 40;
        p.style.setProperty('--dx', Math.cos(angle) * dist + 'px');
        p.style.setProperty('--dy', Math.sin(angle) * dist - 20 + 'px');
        p.style.left = (rect.left + rect.width / 2) + 'px';
        p.style.top = (rect.top + rect.height / 2) + 'px';
        document.body.appendChild(p);
        setTimeout(()=> p.remove(), 1150);
      }
    }

    /* ---------- small surprises: peek, spin, stumble — kept rare ---------- */
    function playEasterEgg(){
      if(botBusy || bot.dataset.state !== 'idle') return;
      const roll = Math.random();
      if(roll < 0.34){
        bot.classList.add('bot-wiggle');
        setTimeout(()=> bot.classList.remove('bot-wiggle'), 1200);
      } else if(roll < 0.6){
        bot.classList.add('bot-fall');
        setTimeout(()=>{
          bot.classList.remove('bot-fall');
          trySay('fall', 'I\u2019m okay! Totally planned that. 😂', 30000);
        }, 700);
      } else if(roll < 0.8){
        const logo = document.querySelector('.logo-mark');
        if(logo){
          const rect = logo.getBoundingClientRect();
          setState('peek');
          idleTarget = { x: rect.left + rect.width / 2 + 30, y: rect.top + rect.height / 2 };
          mouse = null;
          setTimeout(()=>{ if(!botBusy) setState('idle'); idleTarget = pickIdleTarget(); }, 2200);
        }
      } else {
        spawnDataCrystal();
      }
    }
    if(mascotEnabled){
      setInterval(()=>{
        if(!botBusy && Date.now() - lastActivity > 3200 && bot.dataset.state === 'idle' && Math.random() < 0.35){
          playEasterEgg();
        }
      }, 9000);
      setState('idle');
    }

    /* ---------- mini game: catch the data crystal ---------- */
    let crystalActive = false;
    function spawnDataCrystal(){
      if(crystalActive || !mascotEnabled) return;
      crystalActive = true;
      trySay('crystal-intro', 'Catch that data crystal! ⚡', 60000);
      const crystal = document.createElement('div');
      crystal.className = 'data-crystal';
      const cx = Math.min(window.innerWidth - 80, Math.max(40, pos.x + (Math.random() > 0.5 ? 90 : -90)));
      const cy = Math.min(window.innerHeight - 140, Math.max(80, pos.y - 40));
      crystal.style.left = cx + 'px';
      crystal.style.top = cy + 'px';
      crystal.setAttribute('role', 'button');
      crystal.setAttribute('aria-label', 'Catch the data crystal');
      const removeCrystal = ()=>{ if(crystal.parentNode) crystal.remove(); crystalActive = false; };
      crystal.addEventListener('click', ()=>{
        removeCrystal();
        celebrate('YESSS! You got it! 🚀');
        showProjectPrompt();
      });
      document.body.appendChild(crystal);
      setTimeout(removeCrystal, 9000);
    }
    function showProjectPrompt(){
      const el = document.getElementById('contact');
      setTimeout(()=>{
        if(el){ el.scrollIntoView({ behavior: reduceMotion ? 'auto' : 'smooth', block:'start' }); el.classList.add('highlight-pulse'); setTimeout(()=> el.classList.remove('highlight-pulse'), 1700); }
      }, 1200);
    }

    let lastFrame = performance.now();
    let lastTrailSpawn = 0;
    function spawnTrailDot(x, y, speed){
      const dot = document.createElement('div');
      dot.className = 'bot-trail-dot';
      dot.style.left = x + 'px';
      dot.style.top = y + 'px';
      dot.style.opacity = Math.min(0.6, speed / 30);
      document.body.appendChild(dot);
      setTimeout(()=> dot.remove(), 650);
    }

    function loop(now){
      if(!mascotEnabled) return;
      const dt = Math.min((now - lastFrame) / 16.6, 3);
      lastFrame = now;
      const idleFor = Date.now() - lastActivity;

      if(!botBusy){
        if(idleFor > 6500 && bot.dataset.state !== 'celebrate' && bot.dataset.state !== 'excited'){
          setState('sleepy');
          if(idleFor > 9000 && !inactiveNoticeGiven){
            inactiveNoticeGiven = true;
            trySay('inactive', 'Heyyy… don\u2019t leave me alone! 🥺', 20000);
          }
        } else if(idleFor <= 6500 && bot.dataset.state === 'sleepy'){
          setState('idle');
        }
      }

      let goal;
      if(mouse && idleFor < 6500 && !botBusy){
        goal = { x: mouse.x + 48, y: mouse.y - 64 };
      } else if(botBusy){
        goal = { x: window.innerWidth - 130, y: window.innerHeight - 190 };
      } else {
        if(Math.abs(pos.x - idleTarget.x) < 24 && Math.abs(pos.y - idleTarget.y) < 24){
          idleTarget = pickIdleTarget();
        }
        goal = idleTarget;
      }

      const speed = bot.dataset.state === 'sleepy' ? 0.018 : 0.06;
      const dx = goal.x - pos.x, dy = goal.y - pos.y;
      const prevX = pos.x, prevY = pos.y;
      pos.x += dx * speed * dt;
      pos.y += dy * speed * dt;

      const tilt = Math.max(-16, Math.min(16, dx * 0.04));
      bot.style.transform = `translate3d(${pos.x}px, ${pos.y}px, 0) rotate(${tilt}deg)`;
      bot.style.setProperty('--lookx', Math.max(-2, Math.min(2, dx * 0.01)) + 'px');
      positionBubble();

      const moveSpeed = Math.hypot(pos.x - prevX, pos.y - prevY);
      if(moveSpeed > 1.4 && bot.dataset.state !== 'sleepy' && now - lastTrailSpawn > 55){
        lastTrailSpawn = now;
        spawnTrailDot(pos.x + 30, pos.y + 55, moveSpeed);
      }

      requestAnimationFrame(loop);
    }
    if(mascotEnabled) requestAnimationFrame(loop);

    window.addEventListener('resize', ()=>{ idleTarget = pickIdleTarget(); });

    /* ============================================================
       Voice panel: recognition, transcript, KB answers, navigation
       ============================================================ */
    const launcher = document.getElementById('voiceLauncher');
    const panel = document.getElementById('voicePanel');
    const closeBtn = document.getElementById('voiceClose');
    const transcriptEl = document.getElementById('voiceTranscript');
    const statusEl = document.getElementById('vcStatus');
    const hintEl = document.getElementById('vcHint');
    const inputEl = document.getElementById('vcInput');
    const sendBtn = document.getElementById('vcSend');
    const micBtn = document.getElementById('vcMic');
    const suggestionsEl = document.getElementById('vcSuggestions');
    const muteBtn = document.getElementById('voiceMute');

    muteBtn.addEventListener('click', ()=>{
      voiceMuted = !voiceMuted;
      muteBtn.setAttribute('aria-pressed', String(voiceMuted));
      if(voiceMuted && 'speechSynthesis' in window) window.speechSynthesis.cancel();
    });

    const SpeechRecognitionCtor = window.SpeechRecognition || window.webkitSpeechRecognition;
    let recognizer = null;
    let isListening = false;

    if(SpeechRecognitionCtor){
      recognizer = new SpeechRecognitionCtor();
      recognizer.lang = 'en-US';
      recognizer.interimResults = false;
      recognizer.maxAlternatives = 1;

      recognizer.onstart = ()=>{
        isListening = true;
        micBtn.classList.add('listening');
        statusEl.textContent = 'LISTENING…';
        setState('listening');
        addMessage('system', 'Listening…');
      };
      recognizer.onresult = (e)=>{
        const text = e.results[0][0].transcript;
        removeLastSystemMessage();
        handleUserInput(text);
      };
      recognizer.onerror = ()=>{
        removeLastSystemMessage();
        statusEl.textContent = 'READY';
        addMessage('system', "Couldn't hear that clearly — try again or type instead.");
      };
      recognizer.onend = ()=>{
        isListening = false;
        micBtn.classList.remove('listening');
        if(statusEl.textContent === 'LISTENING…') statusEl.textContent = 'READY';
      };
    } else {
      micBtn.disabled = true;
      hintEl.textContent = 'VOICE INPUT NOT SUPPORTED IN THIS BROWSER — TYPE YOUR QUESTION';
    }

    function openPanel(){
      panel.classList.add('open');
      document.body.style.overflow = 'hidden';
      if(!transcriptEl.dataset.greeted){
        transcriptEl.dataset.greeted = '1';
        const greetText = "Heyyy! 👋 I'm listening… what do you want to know?";
        addMessage('bot', greetText);
        speak(greetText);
      }
      setTimeout(()=> inputEl.focus(), 250);
    }
    function closePanel(){
      panel.classList.remove('open');
      document.body.style.overflow = '';
      if(recognizer && isListening) recognizer.stop();
      if('speechSynthesis' in window) window.speechSynthesis.cancel();
      botBusy = false;
      setState('idle');
    }
    launcher.addEventListener('click', openPanel);
    closeBtn.addEventListener('click', closePanel);
    panel.addEventListener('click', (e)=>{ if(e.target === panel) closePanel(); });
    window.addEventListener('keydown', (e)=>{ if(e.key === 'Escape' && panel.classList.contains('open')) closePanel(); });

    function addMessage(role, text){
      const div = document.createElement('div');
      div.className = 'vt-msg ' + role;
      div.textContent = text;
      transcriptEl.appendChild(div);
      transcriptEl.scrollTop = transcriptEl.scrollHeight;
      return div;
    }
    function removeLastSystemMessage(){
      const msgs = transcriptEl.querySelectorAll('.vt-msg.system');
      if(msgs.length) msgs[msgs.length - 1].remove();
    }

    // builds a simple, honest project summary from what the visitor actually said —
    // no invented pricing or specifics, just what was detected in their own words
    const FEATURE_HINTS = [
      { re:/\b(online order|ordering|order online)\b/i, label:'Online ordering' },
      { re:/\b(ecommerce|e-commerce|online store|shop online|sell products)\b/i, label:'E-commerce / online store' },
      { re:/\b(ai|automation|chatbot|assistant)\b/i, label:'AI assistant / automation' },
      { re:/\b(booking|appointment|reservation)\b/i, label:'Online booking' },
      { re:/\b(portfolio|gallery)\b/i, label:'Portfolio / gallery' }
    ];
    function buildLeadSummary(businessType, sourceText){
      const features = FEATURE_HINTS.filter(f => f.re.test(sourceText)).map(f => f.label);
      if(!features.length) features.push('Custom design, mobile-friendly build');
      return {
        business: businessType.replace(/\b\w/g, c => c.toUpperCase()),
        websiteType: 'Business / ' + businessType.replace(/\b\w/g, c => c.toUpperCase()) + ' Website',
        features
      };
    }
    function renderLeadCard(lead){
      const card = document.createElement('div');
      card.className = 'lead-card';
      card.innerHTML =
        '<div class="lc-label">◆ PROJECT IDEA DETECTED</div>' +
        '<dl>' +
          '<dt>Business</dt><dd>' + lead.business + '</dd>' +
          '<dt>Website</dt><dd>' + lead.websiteType + '</dd>' +
          '<dt>Features</dt><dd>' + lead.features.join(', ') + '</dd>' +
          '<dt>Next step</dt><dd>Get a custom quote</dd>' +
        '</dl>' +
        '<div class="lc-actions">' +
          '<a class="lc-btn primary" href="' + WA_LINK + '" target="_blank" rel="noopener">Chat on WhatsApp</a>' +
          '<button class="lc-btn secondary" type="button" data-start-project="1">Start Project</button>' +
        '</div>';
      card.querySelector('[data-start-project]').addEventListener('click', ()=>{
        celebrate('Yessss! PROJECT MODE ACTIVATED! 🚀');
        navigateTo('#contact');
      });
      return card;
    }

    function handleUserInput(text){
      text = (text || '').trim();
      if(!text) return;
      addMessage('user', text);
      inputEl.value = '';
      botBusy = true;
      statusEl.textContent = 'THINKING…';
      setState('thinking');

      // brief "thinking" pause makes the reply feel considered, not instant
      setTimeout(()=>{
        const biz = detectBusinessType(text);
        const match = biz ? null : matchKB(text);
        const replyText = biz ? biz.flavor : (match ? match.answer : FALLBACK);

        statusEl.textContent = 'ANSWERING…';
        setState((biz || match) ? 'happy' : 'confused');
        const msgEl = addMessage('bot', replyText);

        speak(replyText, ()=>{
          statusEl.textContent = 'READY';
          botBusy = false;
          setState('idle');
        });

        if(biz){
          burstParticles();
          navigateTo('#services');
          setTimeout(()=> msgEl.appendChild(renderLeadCard(buildLeadSummary(biz.businessType, text))), 150);
        } else if(match){
          burstParticles();
          if(match.target) navigateTo(match.target);
          if(match.cta){
            setTimeout(()=>{
              const link = document.createElement('a');
              link.href = WA_LINK;
              link.target = '_blank';
              link.rel = 'noopener';
              link.className = 'contact-btn';
              link.style.marginTop = '4px';
              link.style.display = 'inline-flex';
              link.textContent = 'Open WhatsApp →';
              msgEl.appendChild(document.createElement('br'));
              msgEl.appendChild(link);
            }, 50);
          }
        }
      }, 550);
    }

    micBtn.addEventListener('click', ()=>{
      if(!recognizer) return;
      if(isListening){ recognizer.stop(); return; }
      if('speechSynthesis' in window) window.speechSynthesis.cancel();
      try{ recognizer.start(); }catch(err){ /* already started */ }
    });
    sendBtn.addEventListener('click', ()=> handleUserInput(inputEl.value));
    inputEl.addEventListener('keydown', (e)=>{ if(e.key === 'Enter') handleUserInput(inputEl.value); });
    suggestionsEl.addEventListener('click', (e)=>{
      const chip = e.target.closest('.vc-chip');
      if(chip) handleUserInput(chip.dataset.q);
    });
  })();
</script>

</body>
</html>

